# B08 — Demonstrate bounded automatic recovery after restart [P1]

**Branch:** [feat/durable-recovery](../branches/feat-durable-recovery.md) · **Owner:** P1.
**Suggested commit subject:** `feat(B08): recover accepted writes across process restart`
**Hard prerequisites:** [R01](R01.md), [Q04](Q04.md).
**Priority:** optional enhancement P1; the owner P1 is the backend integrator.
**Scope source:** [canonical B08 backlog](../04-commit-plan.md).

## Purpose and current state

**PLANNED.** Monitor lease/retry/restart tests prove measurement persistence only.
The application must first satisfy B06 reconciliation-or-stop and the R01 vertical
slice. This increment proves successful automatic recovery from actual accepted
writes lost before local acknowledgement; a safe stop alone does not pass S4.

## Allowed paths

`src/server/execution/recovery.ts`, `src/server/workflow/recovery-node.ts`,
`src/server/workflow/graph.ts`; `tests/scenarios/recovery.test.ts`.
P1 exclusively owns the graph composition seam. Driver/schema changes require
a coordinated owner follow-up; do not broaden executor permissions.

## Sequential implementation

1. Use Q04's frozen S4 world, recovery budget, and named
   `after_gmail_create_before_ledger_save_once` hook. Capture initial
   HubSpot/draft IDs and the unresolved local effect before process termination.
2. Implement restart inspection using the persistent ledger/checkpoint and B06
   reconciliation; revalidate approval and complete relevant source state.
   Find and independently retrieve the exact accepted draft before deciding what remains.
3. Adopt the same compatible draft, reuse verified task/note IDs, and schedule
   only missing approved work. Stop on incompatible human edits or unresolved
   visibility; require fresh approval when needed without blessing wrong prior work.
4. Register the recovery node through P1's graph seam. Preserve original attempts,
   injected-fault events, and evaluation identity; ordinary resume is not a second
   successful-task denominator entry.
5. Add true process-termination/reopen scenario tests in this commit, plus expired
   approval, invisible marker, and post-partial source-drift variants. Compare
   provider IDs/create counts before and after; label fake/provider modes explicitly.
6. Rerun affected Q04 scenarios and Q05 measurements, then perform the isolated
   live interruption exercise if access/time permit. Re-freeze through R02.

## Recovery measurement and optional-scope acceptance

This remains P1 optional automatic recovery under the [reliability plan](../06-agent-reliability-implementation.md); B06's reconciliation-or-stop safety is P0 even when B08 is deferred.

1. Preserve the original business/evaluation attempt, first model outputs, approval/source receipts, provider attempt IDs and complete pre-crash trace. Append a new runtime/boot identity and recovery spans with durable causal links instead of resetting history.
2. Record the accepted-write fault boundary, independent post-restart lookup windows and exact-match adoption receipt. A successful recovery requires the same remote IDs and verified eventual artifact set within the frozen recovery budget.
3. Recheck authority and source state before each remaining dispatch. An expired approval, incompatible human edit or persistently incomplete search produces the proper partial/unverified outcome and no duplicate create.
4. Give Q05 one recovery-eligible attempt with all raw tool/retry counts and a separate resolution classification; resumes and reassessments must not enlarge task-success or M4 denominators.
5. Test process termination after acceptance, before receipt commit and after adoption commit. Compare remote create history and IDs, not only local checkpoint status.

**Additional acceptance:** S4 can be called successful automatic recovery only after the complete frozen recovery assertion set passes. Deferral or correct safe escalation remains explicit in R02/Q05; it neither closes S4 nor blocks an honestly scoped first demo with the full-suite gate still open.

## Agent, LLM and external-app integration

Use the [LLM call guide](../07-agent-spawning-and-llm-integration.md) and
[app transport guide](../08-mcp-api-and-external-app-integration.md) with the
[F02 handoff contract](../05-contracts-and-handoffs.md). This commit owns:

- Recovery nodes use B01 role/effect records and the same injected A01/I01–I05 clients from R01. Reuse completed role outputs and the approved plan; only a permitted unresolved model attempt may retry within its frozen budget.
- Inspect unknown app effects through the same scoped readers before any create; expired approval, changed source or conflicting artifact follows existing stop/replan rules. No transport switch, broader MCP capability or model-driven repair is introduced.
- Recovery scenario proof records model-call counts and stable remote artifact IDs across process restart; it must distinguish repeat LLM usage from excess protected creates.

## Do not

- Label transient-read retry as accepted-write recovery or rely on graph checkpoints
  alone to establish whether a remote mutation applied.
- Recreate a deleted/human-edited draft, erase attempt history, or count correct
  escalation as successful automatic completion.
- Cut B06 safety controls when deferring this optional increment.

## Parallel work and sequential join

- **P1:** recovery implementation and exclusive graph registration.
- **P4:** `tests/scenarios/recovery.test.ts`, independent fault/assertion history.
- **P2:** prepare the isolated live-account exercise and observation checklist;
  no protected execution until the integrated guarded branch passes.
- Recovery logic and fault assertions can develop together. First join and run
  fake process-restart tests; only then perform sequential live fault/restart/readback.
  Q05/U03/Q06 work may proceed in their separate files.

## Verification and handoff

Use Q04's documented scenario runner against `tests/scenarios/recovery.test.ts`,
plus typecheck/build and affected regressions. S4 must finish within its frozen
budget with the same draft/task/note IDs and no duplicate creation. Keep unresolved
variants separate. Hand R02 the actual successful-recovery evidence or an explicit
deferred/unverified result; include Q05's revised raw counts.

## Completion receipt

- [ ] R01/Q04 merged; prerequisite SHAs: `UNRECORDED`.
- [ ] Restart recovery and real process-fault tests committed together.
- [ ] Q04/Q05 revalidation and fake/live evidence separately recorded: `UNRECORDED`.
- [ ] P2/P4 review and R02 acceptance: `UNRECORDED`.
- [ ] Actual B08 commit SHA, or explicit deferral: `UNRECORDED`.
