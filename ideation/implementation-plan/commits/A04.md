# A04 — Add the Blind Semantic Auditor

**Purpose:** Check proposed customer text against original sources and flag unsupported, contradictory, or omitted claims before review progression.

- **Branch / owner:** [`feat/agent-auditor`](../branches/feat-agent-auditor.md) / P3.
- **Hard merge prerequisites:** [A01](A01.md). Preserve the [source backlog](../04-commit-plan.md)'s gates; early fixture work does not waive them.
- **Suggested commit subject:** `feat(agents): add independent-context semantic auditor [A04]`
- **Priority:** P0. This is a future implementation task, not an executed commit.

## Current state and reuse

No product auditor exists. A01/F02 supply the future role interfaces; the [monitor's post-run assessments](../../../src/server/monitoring/assess.ts) and supplied human labels are a separate measurement path, not this preapproval agent. Develop against fixture drafts while A03 is pending.

The app/agent/provider portions of F02 and the product runtime remain unfinished.
Use Q01 fixtures when available through their owner; Q01 is not an added hard merge
gate for this ID. No live provider or model call is claimed by this document.

## Allowed paths

- `src/server/agents/auditor/index.ts`
- `src/server/agents/auditor/prompt.ts`
- `src/server/agents/auditor/validate.ts`
- `tests/app/auditor.test.ts`

Only the listed paths belong to this commit. Ask the owning branch for a shared
interface change before integrating dependent code; do not fork that interface.

## Sequential implementation steps

1. Implement prompt.ts with original source projections, proposed customer text, claim references, and the task contract. Explicitly exclude analyst/drafter reasoning, confidence, shared history, and an expected verdict.
2. Implement validate.ts for structured findings, supported reference IDs, affected claims, and required verdict fields. Keep an unavailable or malformed required stage distinct from a clean audit.
3. Implement index.ts through A01 with bounded retries and persisted original findings. Return concerns to deterministic workflow routing; expose no approval or artifact-repair operation.
4. Add context-exclusion tests and independently labeled sound/unsound/incomplete draft cases in auditor.test.ts. Test required-stage timeout/refusal/invalid output without silently skipping the auditor.
5. Run targeted tests and F01 build/typecheck. When actual auditor outputs exist, compare them with independent labels to count missed defects and false blocks; hand concerns/failures and typed results to R01/Q05.

## Exact auditor invocation and authority boundary

Follow [agent/LLM integration](../07-agent-spawning-and-llm-integration.md).
Export `auditSemantics(input: AuditorInput, ctx: AgentInvocationContext, deps)`
from **auditor/index.ts**, returning `AgentCallResult<AuditFindings>` through F02
schemas. R01's **src/server/workflow/nodes.ts** invokes it in `audit_semantics`
only after drafting and B03's deterministic proposal checks succeed.

**The actual request path is** graph node → `auditSemantics` → A01 `invokeRole`
→ A01 `dispatchStructured` → `ChatOpenAI`/OpenAI Responses. **auditor/prompt.ts**
builds an independent context from original GitHub/HubSpot source projections,
all proposed customer-text entries, claim references and the supported task
contract. One logical invocation checks the whole fixed selected set; findings
identify their commitment and claim, with no omitted or extra draft scope.
**auditor/validate.ts** checks structured findings after raw-response persistence.
Exclude the analyst's narrative/rationale, drafter reasoning/confidence, shared
messages, prior verdict and desired answer; enforce exclusion in tests rather
than rely on the prompt to ignore leaked fields.

Use `role=auditor`, stable run/revision/role identity, and immutable source/draft
digests. A01 resolves optional `OPENAI_MODEL_AUDITOR` and enforces the same
recorded transport/schema budget. This role has no separate retry owner or model
client. Return findings and first-output references to R01; concerns route to
`safely_blocked`, required-stage exhaustion/refusal/unavailable output to `failed`
before protected effects, and a validated clear finding merely allows plan freeze.

Original sources come from R01's deterministic adapters; this role performs no
MCP/API app reads or writes. Slack approval belongs to B05/I04, protected execution
to B06, and fresh verification to B07 through the
[external-app boundary](../08-mcp-api-and-external-app-integration.md).
No auditor result may send a Slack approval, call a provider, repair a draft,
waive freshness, or become Q05's independent human label.

**Additional invocation acceptance:** inspect the exact A01 messages and verify
excluded content is absent; assert one logical call per valid role/revision,
zero model calls during ordinary approval resume, and preserved raw findings on
invalid output. R01 tests that blocked/failed audits never reach plan approval or
protected dispatch, including the enabled-auditor outage case.

## Auditor usefulness and measurement independence

Keep the preapproval auditor separate from the independent evaluation described in the [reliability plan](../06-agent-reliability-implementation.md).

1. Record the exact blind input projection and first auditor output via A01, including source/output digests and prompt/model/schema versions. Persist findings and failures before any correction/rerun.
2. Present findings as claim-specific concerns with original source references and reasons. A clean structured response only permits deterministic progression; it does not authenticate a source, approve a write or establish the final state.
3. Supply Q05 with both the proposed text and auditor verdicts for comparison against independently supplied human labels. Distinguish missed defects, correct detections and false blocks; do not score the auditor against its own generated labels.
4. Extend tests with a syntactically valid but unsupported citation, an omitted uncertainty statement, a correct draft that should not be blocked, leakage of drafter rationale/confidence and required-stage timeout/refusal.
5. Preserve enabled-stage failures in the canonical trace and prevent review progression after exhaustion. A successful later correction cannot erase the original auditor's missed defect or false block.

**Additional acceptance:** context-exclusion and deterministic routing tests pass; actual auditor usefulness remains unverified until human-labeled real outputs exist. Hand R01 concerns/failure routing and Q05 the raw comparison evidence without broadening auditor authority.

## Do not

- Do not feed the auditor another role's rationale/confidence, give it the desired answer, or let its verdict grant approval or override deterministic guards.
- Do not treat a clean audit as factual proof, a model-generated label as human ground truth, or a failed enabled auditor as optional during a run.
- Do not edit A03, A01, shared schemas, dependency files, storage, or graph composition on this branch.

## Parallel subtasks and sequential joins

- P3 owns auditor/index.ts and auditor/prompt.ts; a named role delegate may own auditor/validate.ts.
- P4 owns tests/app/auditor.test.ts and independent context-exclusion/semantic fixtures.
- A02/A03 developers work on their role directories concurrently; each reviewer receives original sources rather than another agent's self-assessment.

**Join:** Freeze the blind context projection → parallel prompt, finding validation, and independent fixtures → integrate A01 → tests → R01 wires this stage after the drafter and deterministic checks.

Assign delegates only when capacity exists; one person owns each listed file.
Development parallelism does not alter the runtime agent order or serial guarded
HubSpot task → note → Gmail draft → GitHub comment → Slack finalization.

## Verification and handoff

Excluded reasoning is absent from the model input, concerns stop review progression, and exhausted required-stage failures remain failures. Report actual false blocks/missed defects only after labeled runs; fixture successes do not prove the live auditor works.

Module conformance can be reviewed with deterministic fixtures. Q05's independently
labeled actual-model evidence is a separate quality claim, not an added hard merge
prerequisite for this module.

After F01 provides the proposed application runner, use
`npm run test:app -- tests/app/auditor.test.ts`, followed by the agreed typecheck/build gates.
That application-test command is planned, not currently installed by the standalone
monitor. Preserve relevant existing monitor/checker regressions when the F02 event
seam is exercised. Report fixture, model, and live-provider evidence separately.

**Consumers:** [R01](R01.md), [Q05](Q05.md). Include the normalized interface/artifact, exact
commands and exit codes, fixture/model/policy versions as relevant, and failed or
unrun gates. Store private IDs/raw evidence outside committed public examples.

## Completion record

- [ ] Implementation completed; owned-path diff reviewed.
- [ ] Verification completed; commands, exit codes, and mode recorded: ______
- [ ] Independent review completed; reviewer/date: ______
- [ ] Merged SHA recorded: ______
- [ ] Evidence linked; failed/unrun model or live smoke gates explicit: ______
- [ ] Consumer handoff accepted; P1 received the global-status update: ______
