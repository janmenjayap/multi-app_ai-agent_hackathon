# Q01 — Freeze scenario worlds, expected outcomes, and controllable fakes

**Purpose:** Give parallel implementations a shared, independently fixed world and fault contract for the 18 scenario families.

- **Branch:** [`feat/evaluation-fixtures`](../branches/feat-evaluation-fixtures.md).
- **Owner:** P4 — independent fixture/evaluation owner.
- **Priority:** P0 for the promised application and its evidence.
- **Suggested git commit subject:** `test(evaluations): [Q01] freeze scenario oracles and stateful fakes`.
- **Hard merge prerequisites:** [F02](F02.md).

The synthetic fixture implementation is recorded in the completion receipt below; downstream workflow execution remains pending. [P00](P00.md)
preserves the current monitor before foundation work. See the
[canonical commit plan](../04-commit-plan.md), [handoff contracts](../05-contracts-and-handoffs.md),
[agent reliability completion plan](../06-agent-reliability-implementation.md),
[frontend pipeline and reliability guide](../09-frontend-pipeline-and-reliability.md),
and [current monitor guide](../../../tools/monitoring/README.md).
Code may be drafted against frozen interfaces before prerequisites merge;
acceptance and live execution still wait for the listed gates.

## Current state and reuse

The existing [manifest schemas](../../../src/shared/reliability.ts) already validate complete S1 artifact declarations, and [demo.ts](../../../src/server/monitoring/demo.ts) generates a synthetic observation stream. Neither executes a stateful fake application. Reuse the validator; generated human labels are format examples, not actual review.

## Allowed paths

Keep this commit within these paths from the canonical plan. Existing paths are
reused; absent paths are proposed additions. Shared-file changes belong to their
foundation owner and must land before consumers.

- `tests/fixtures/world.json`
- `tests/fixtures/scenarios.json`
- `tests/fixtures/source-labels.json`
- `tests/fixtures/faults.ts`
- `tests/fakes/providers.ts`
- `tests/fakes/model.ts`
- `src/server/evaluations/manifest.ts`
- `tools/fixtures/seed.ts`
- `tools/fixtures/reset.ts`
- `tests/app/manifest.test.ts`

## Sequential implementation steps

1. Freeze the complete suite census in `scenarios.json`: 18 family baselines, the six families requiring four additional attempts, named safety variants, and correction/repair legs. Store scenario, repetition, variant, eligibility, expected terminal state, fault boundary, budgets, versions, and mode separately from attempted execution records. The 42 baseline/repetition slots are targets, not observations.

2. Independently author source facts, forbidden claims, semantic content invariants, code-selected owner/recipient/status constraints, allowed effects, protected Beta records, and stage boundaries before any model invocation. Exact generated draft bytes do not exist yet and must not be invented as a pre-run oracle. Retain oracle reviewer identity/reason and source snapshot digests. Generated positive `human` labels stay synthetic fixtures.

3. Reuse the manifest validator under F02's observation schema v2. Freeze a logical manifest/hash using `EffectIdRef` for future-created provider objects and distinct `ApprovedContentRef` for generated exact content. B03 binds the latter to its immutable approved plan/bytes before any dispatch; Q02 resolves that frozen plan receipt, never provider-returned text. Independently observed unique marker-to-provider-ID receipts resolve only object IDs. A separately hashed resolved export never replaces the original manifest hash or relaxes source facts/semantic invariants.

4. Build stateful provider fakes independent of executor caches and expected-output generation. Reader APIs must expose records/history, pagination, actor/time metadata, and missing scopes. Support wrong-field acknowledgements, page-two failure, accepted unknown writes, duplicate creations, later human drift, and phantom Slack success. A fixed fake model remains simulated mode.

5. Add operator-only scoped seed/reset utilities with exclusive namespace ownership. Record setup separately from scored windows; source edits and repair stages are actor-tagged and predeclared. Agent capabilities cannot call cleanup utilities.

6. Add manifest/fake tests that reject absent artifacts, incomplete required fields, mutable oracles, ambiguous ID bindings, content bound after dispatch or from provider output, omitted cohort slots, and caller-generated review provenance. Include `no_affected` manifests whose complete source/selection evidence proves zero eligible commitments and no protected writes without requiring plan/approval/Slack artifacts. Verify deterministic fault replay and the original monitor/checker regressions. Hand F02-valid canonical scenario/world examples and their identities to every lane; U01 separately owns browser-only state fixtures and does not wait for Q01 to merge.

## Agent, LLM and external-app integration

Use the [LLM call guide](../07-agent-spawning-and-llm-integration.md) and
[app transport guide](../08-mcp-api-and-external-app-integration.md) with the
[F02 handoff contract](../05-contracts-and-handoffs.md). This commit owns:

- `tests/fakes/model.ts` implements the A01 injected model seam; `tests/fakes/providers.ts` implements F02's four narrow adapters. Keep model outputs separate from stateful provider reads so a successful write acknowledgement cannot manufacture matching observed state.
- Freeze the suite's model mode and per-app fake/REST (later explicitly qualified MCP) transport configuration, model/prompt/schema versions, role order/call expectations and selected account scope before execution. Seed malformed/refused model output, timeout, tool errors and incomplete pagination in `faults.ts`.
- Freeze semantic requirements before generated text exists; exact approved content binds later through B03. Fixture seed/reset tools are operator-only external-app utilities outside scored execution; their credentials and cleanup methods never enter agent clients.

## Do not

- Do not derive expectations from whichever artifacts were produced, modify a baseline after failure, or mark generated human labels as independently reviewed.
- Do not label a fixed fake model as model-with-fake-providers; that mode requires an actual model invocation. Do not claim the 18-family workflow suite ran merely because fixtures exist.

## Parallel subtasks and sequential joins

- P4 owns `world.json`, `scenarios.json`, and
    `src/server/evaluations/manifest.ts`; a reviewer independently owns
    `source-labels.json`.
- P2, when available, owns `tests/fakes/providers.ts` and `tools/fixtures/seed.ts`/`reset.ts`; these remain on Q01's branch.
- P3 or a helper owns `tests/fakes/model.ts` and `tests/fixtures/faults.ts`; the test partner owns `tests/app/manifest.test.ts`.

**Join:** Source/oracle review precedes fake behavior. Provider/model fake implementations may then run in parallel. Q01 and U01 may proceed concurrently after F02; U01 owns browser fixtures and adopts Q01's reviewed scenario identities when available without importing unmerged files or changing UI state semantics. P4 joins and versions canonical fixtures before B02/A modules/Q02 consume them; shared schema edits go through P1/F02.

Names P1–P4 identify responsibility lanes, not extra people. With fewer teammates,
one person rotates through ready subtasks; never assign two simultaneous owners
to the same file. Tests and their implementation belong in this same commit.

## Verification and handoff

Attach fixture review, rejected incomplete-manifest cases, deterministic fault
checks, and existing monitor regression results. Hand U01 the reviewed scenario
and evidence identities to adopt in its browser-owned fixtures, and give policy,
adapters, agents and Q04 the same versions; no live seed or rendered UI result is
implied.

Record exact commands and evidence mode. Reuse current `npm run typecheck`,
`npm run build`, `npm run test:monitor`, and `npm run test:checker` where relevant;
application-specific test commands must first be introduced by F01.
A command exit code alone is insufficient: inspect named tests, assessments,
required artifacts, and failed/unrun cases.

## Required reliability proof

- [x] The full suite census and logical manifest are immutable independently of executor output.
- [x] `EffectIdRef` binding changes object references only; `ApprovedContentRef` binds exact bytes from B03 before dispatch while pre-run semantic invariants and original-output judgments remain independent.
- [x] Source-label fixtures disclose their provenance; actual human review is still a separate required step.

## Completion record

- [x] Hard prerequisite commit SHAs recorded and reviewed.
- [x] Implementation and meaningful tests land together within the ten allowed paths.
- [x] Required verification and consumer contract review recorded; unrun downstream cases disclosed.
- [x] Actual commit SHA: `bcced55d61b90b2ce69e077ecc0475b814e27335`.
- [x] Evidence/report references and raw counts recorded below.
- [x] Owner: Codex P4; reviewer: delegated `q01_merge_review`; September 14, 2026 (IST).
- [x] P1 integrator updates [Global Scale](../Global%20Scale.md) for demonstrated scope only.


## Completion receipt

Conventions **1.2**, application/observation schema **2**, original implementation
base `7ee74a7c651a9aee9efd7928c784cf7525efbb3b`, hard F02 prerequisite
`87d3c7d7bad17d54300e452e932c7f226fc74299`. Q01 implementation commit:
`bcced55d61b90b2ce69e077ecc0475b814e27335` on `feat/evaluation-fixtures`.
The user authorized merging into main and publishing both branches. An isolated
checkout preserved concurrent B01 work and integrated the already committed U01
and I01 histories. Q01's implementation commit contains exactly its ten allowed
files; this separate integrator receipt updates the plan and global register.

Frozen suite `promiseguard-q01-v1`, world `promiseguard-demo-v1`, source oracle
`promiseguard-source-oracle-v1`; suite SHA-256:
`a58137ad7a59377cdc110af3c917b8edecc295c280dac6d2a3bd6ae2026def77`.
The census contains **18 family baselines**, **24 additional repetitions**,
**46 named variants**, and **4 repair legs**: **92 targets**, including the
required 42 baseline/repetition slots. No census entry has been executed by the
application. Actual workflow attempts, live model/provider calls, and actual
human semantic reviews remain **zero**.

The independent source reviewer authored source facts and constraints before
fake outputs. The merge reviewer accepted allowed paths, frozen source/suite/
model/fault hashes, prerequisite ancestry, synthetic provenance, and imports
that depend only on F02 or Q01. These are delegated agent code reviews, not
human semantic labels. The latter remain pending.

Fresh isolated merge-candidate validation used Node **24.21.0**, with
`/tmp/node-v24.21.0-linux-x64/bin` prepended to PATH:

- `npm run test:app -- tests/app/manifest.test.ts`: **18/18 passed**.
  Covers census/oracle immutability, omitted slots, absent artifacts/fields,
  forged review provenance, no-affected evidence scope, partial artifacts,
  pre-dispatch approved content, ambiguous ID bindings, deterministic faults,
  independent provider state, model retries, and operator repair.
- `npm run typecheck`: **passed**.
- `npm run build`: **passed**, server and frontend.
- `node --test --test-isolation=none tests/reliability/*.test.mjs tests/monitoring/*.test.mjs`:
  **129/129 passed**, zero failures or skips. CLI subprocess access used the
  approved execution context. Local log: `/tmp/promiseguard-q01-merge-baseline.log`.
- `git diff --cached --check`: **passed**; all ten copied source hashes matched
  the reviewed working files before commit.

Earlier implementation verification also exercised operator-only seed/reset:
exclusive namespace creation, wrong-owner rejection, stale-lease rejection,
and matching cleanup all passed. The utilities require a server build and
Node 24; they operate only on local synthetic fixture files. No live account
seed/reset, application scenario execution, or browser suite was run for Q01.
Historical monitor-v1/checker-v1 evidence remains unchanged.

Consumer handoff: `loadFixtureSuite()` and `createScenarioManifest()` provide
immutable F02-valid census/world/manifest identities. `scenarioWorld()` applies
only frozen setup edits. `bindApprovedPlan()` checks immutable source scope,
exact plan bytes and pre-dispatch binding; `resolveManifestExport()` preserves
the logical hash while independently binding object IDs. Q02 still owns trusted
evidence ingestion and actual receipt resolution. `createFakeProviders()` gives
separate mutation/read/observer/operator handles; only the operator handle
exposes cleanup/source repair. `createFakeModel()` implements F02's injected
`AgentDependencies` seam in explicit mock mode.

B02/B03, A01-A04, Q02/Q04, and U01 can consume these versions. U01's separate
browser fixtures remain synthetic and need not import Q01. R01/Q04 execution,
actual source/output human review, live providers/models, and measured release
acceptance remain open. No merged fixture alone closes those gates.
