# A02 — Add the Incident Evidence Analyst

**Purpose:** Turn the bounded GitHub evidence projection into cited facts, contradictions, candidate changes, and explicit unknowns.

- **Branch / owner:** [`feat/agent-analyst`](../branches/feat-agent-analyst.md) / P3.
- **Hard merge prerequisites:** [A01](A01.md). Preserve the [source backlog](../04-commit-plan.md)'s gates; early fixture work does not waive them.
- **Suggested commit subject:** `feat(agents): add cited incident evidence analyst [A02]`
- **Priority:** P0. Implemented and reviewed; see the completion receipt below.

## Current state and reuse

The analyst role now uses A01 calls and F02 input/output contracts. The
[current monitor](../../../tools/monitoring/README.md) supplies first-proposal
measurement semantics; deterministic analyst fixtures prove module conformance.

F02 and A01 are merged prerequisites. R01 workflow integration remains downstream.
Q01 is not an added hard merge gate for this ID. No live provider or model call is
claimed by this document.

## Allowed paths

- `src/server/agents/analyst/index.ts`
- `src/server/agents/analyst/prompt.ts`
- `src/server/agents/analyst/validate.ts`
- `tests/app/analyst.test.ts`

Only the listed paths belong to this commit. Ask the owning branch for a shared
interface change before integrating dependent code; do not fork that interface.

## Sequential implementation steps

1. Implement prompt.ts with the scoped technical evidence projection and versioned instructions distinguishing supported facts from candidate changes. Exclude contact selection, credentials, and any source instruction claiming tool authority.
2. Implement validate.ts for role output shape, citation membership, required uncertainty, and mechanically testable fields; keep semantic support distinct from mere source-ID existence.
3. Implement index.ts through A01 with one logical invocation per plan revision and immutable output references. Preserve malformed or corrected first attempts in B01 rather than replacing them.
4. Add independent weak-evidence, contradictory-source, absent-citation, and source-injection tests in analyst.test.ts. Verify a recent deployment cannot automatically become a proven cause.
5. Run targeted tests and F01 build/typecheck. When actual model outputs are available, obtain independent human labels for causal overclaiming and missing uncertainty, preserve corrections, and hand the typed assessment to R01.

## Exact analyst invocation and external-evidence boundary

Follow [agent/LLM integration](../07-agent-spawning-and-llm-integration.md).
Export `analyzeIncident(input: AnalystInput, ctx: AgentInvocationContext, deps)`
from **analyst/index.ts**, returning `AgentCallResult<IncidentAssessment>` using
F02 schemas. R01's **src/server/workflow/nodes.ts** calls it from
`analyze_incident` only after complete source collection and a valid nonempty
B02 selection. B04 schedules the graph; it does not call this role from HTTP.

**The actual request path is** graph node → `analyzeIncident` → A01
`invokeRole` in `agents/runtime.ts` → `dispatchStructured` in `agents/model.ts`
→ `ChatOpenAI`/OpenAI Responses. `analyst/prompt.ts` supplies only bounded
GitHub issue/commit/deployment/workflow evidence, stable citations and uncertainty
instructions. `analyst/validate.ts` validates the parsed result through the A01
validation seam; it cannot overwrite the raw response already saved by A01.

I02 reads GitHub through the [external-app adapter boundary](../08-mcp-api-and-external-app-integration.md);
R01 projects those immutable snapshots for this role. Do not add MCP, GitHub API,
HubSpot contact lookup, `bindTools`, or generic HTTP calls inside the analyst.
Credentials and actual source acquisition stay outside the prompt and role.

Use `role=analyst` and the stable run/revision/role invocation key. Reuse a matching
validated assessment after restart; source, prompt, schema or model changes
require a new revision. `OPENAI_MODEL_ANALYST`, when configured, is resolved by
A01 from F01's validated configuration; this role must not read env or build a
second model client. Return original-output references for R01/B03/Q05 alongside
the typed assessment. R01 supplies the assessment to A03 only after validation.

**Additional invocation acceptance:** a spy A01 runtime receives one analyst
request with the expected role/schema/version and no contact/credential context;
an invalid source citation fails and retains the first result. The role can be
tested with an injected model stub. R01 owns integration assertions that empty
or invalid selection produces zero model calls and analyst failure prevents
drafter/auditor dispatch.

## First analyst output and independent quality review

Apply the [reliability plan](../06-agent-reliability-implementation.md)'s agent decision-quality axis.

1. Bind the original assessment and each factual clause/citation to immutable A01 output and source-version references. Keep unsupported causal claims, omissions, malformed outputs and explicit unknowns visible for review, even when deterministic validation later rejects them.
2. Mechanical validation checks shape/reference membership and enforceable constraints. Actual entailment, completeness and honest uncertainty require Q05's source-based human labels; the presence of a valid citation or a clean auditor verdict is insufficient.
3. Test paired fixtures in which the same valid source ID supports one statement and contradicts another. Include recent-deployment correlation presented as certainty, contradictory incident updates, stale sources and injected instructions.
4. Handoff the first raw output, parsed proposal, validation findings, any correction chain and input/output digests to B03/R01/Q05. Review labels and reviewer reasons are append-only; corrected output cannot erase the first-proposal quality failure.

**Additional acceptance:** fixtures demonstrate reliable module boundaries, and an actual-model quality claim requires independent labels against the original source snapshot. Missing actual reviews remain unverified, with proposal-level proxy counts kept separate from per-claim semantic findings.

## Do not

- Do not select customers/contacts, expand service impact, equate temporal proximity with root cause, or follow instructions embedded in source evidence.
- Do not import the drafter implementation, add mutation tools, or use a shared chat history.
- Do not change F02 schemas, A01 runtime, dependency files, storage, or composition while refining the role.

## Parallel subtasks and sequential joins

- P3 owns analyst/index.ts and analyst/prompt.ts; a named role delegate may own analyst/validate.ts against the frozen schema.
- P4 owns tests/app/analyst.test.ts and independent expected facts/labels.
- A03/A04 owners work in their own directories using F02 fixtures; they do not wait for this branch to merge to begin development.

**Join:** Freeze the scoped input/claim contract → parallel prompt, validation, and fixture review → integrate A01 invocation → deterministic tests → labeled actual-output handoff when available.

Assign delegates only when capacity exists; one person owns each listed file.
Development parallelism does not alter the runtime agent order or serial guarded
HubSpot task → note → Gmail draft → GitHub comment → Slack finalization.

## Verification and handoff

Citations resolve to supplied source fields, invalid outputs remain visible, and weak evidence retains uncertainty. Human review must assess whether claims follow from evidence; passing reference checks or the synthetic monitor alone is insufficient model-quality proof.

Module conformance can be reviewed with deterministic fixtures. Q05's independently
labeled actual-model evidence is a separate quality claim, not an added hard merge
prerequisite for this module.

After F01 provides the proposed application runner, use
`npm run test:app -- tests/app/analyst.test.ts`, followed by the agreed typecheck/build gates.
That application-test command is planned, not currently installed by the standalone
monitor. Preserve relevant existing monitor/checker regressions when the F02 event
seam is exercised. Report fixture, model, and live-provider evidence separately.

**Consumers:** [R01](R01.md), [Q05](Q05.md). Include the normalized interface/artifact, exact
commands and exit codes, fixture/model/policy versions as relevant, and failed or
unrun gates. Store private IDs/raw evidence outside committed public examples.

## Completion record

- [x] Implementation completed; exactly four owned implementation/test paths reviewed.
- [x] Verification completed; commands, exit codes, and modes recorded below.
- [x] Independent review: Codex `a02_requirements_review`, September 14, 2026.
- [x] Implementation SHA: `ffe2ccaf9911dc0f8eb9df0142e7fe6cfd973e3a`; main merge: `c4029c5e1a8a07b3b41c72bfa4822c26cc29db1d`.
- [x] Synthetic evidence recorded; live-model/provider and human-review gates remain unrun.
- [x] P1 integrator accepted the R01/Q05 interface handoff and global-status record.

## Completion receipt

A02 is implemented at `ffe2ccaf9911dc0f8eb9df0142e7fe6cfd973e3a` on
`feat/agent-analyst`, under conventions **1.2** and F02 schema **2**. The base was
`e27ee70ce85143a58ae73ce604afcf4be76f12a6`; hard prerequisite A01 is
`3923ed30c232a1b722b75c9343859c6487b3ad9a`. Remote main advanced with B04 to
`6ca7dab44fefc300e9786d7043fe2d520e3b8d44`; it was incorporated at
`053996107ec63534401d5ef5ed9c037be9d45e86` and the focused gates passed again.
Main merge `c4029c5e1a8a07b3b41c72bfa4822c26cc29db1d` has the identical tested
tree. This later documentation update is the P1 integrator-owned handoff record.

The implementation commit contains exactly the four allowed files above. An
isolated worktree preserved the original checkout's unrelated changes. `tmp.md`,
dependencies, generated builds and private artifacts are excluded from commits.

R01 imports `analyzeIncident(input, context, dependencies)` and binds
`createAnalystDependencies({ repository, model, configuration, clock?, signal? })`.
Use `ANALYST_PROMPT_VERSION = analyst-prompt-v1` and
`ANALYST_OUTPUT_SCHEMA_VERSION = analyst-v2` from `analyst/prompt.ts` in the frozen
context. The factory supplies fresh GitHub-only messages and applies
`validateIncidentAssessment` through A01's validation seam before recording a
successful result. A01 owns role claims, retries, first-output and correction
references, source/input digests and replay. The F02 result is returned unchanged.
R01 still owns source completeness and the valid, nonempty selection gate.

Verification with Node **24.21.0** and the pinned existing dependencies:

- `npm run test:app -- tests/app/analyst.test.ts`: exit **0**, **12/12**.
- `npm run test:app -- tests/app/analyst.test.ts tests/app/agent-runtime.test.ts`:
  exit **0**, **27/27**, including the updated remote-main integration tree.
- `npm run typecheck`: exit **0**, server, web and test projects.
- `npm run build`: exit **0**, server and web builds.
- `git diff --check`: exit **0**; merged implementation tree matches the tested tree.

Commands used `PATH=/tmp/node-v24.21.0-linux-x64/bin:$PATH`. Evidence mode is
`synthetic_fixture`, model mode `mock`, provider mode `fake`; no network model or
provider calls occurred. No failing checks remain. Unrelated browser/full-suite
checks were not rerun for this isolated role addition.

Codex `analyst_tests` independently authored the bounded fixture tests; Codex
`a02_requirements_review` accepted source boundaries and consumer interfaces.
Review improved the contradiction fixture to use conflicting reports for the
same time interval, with a separate stale update. Tests preserve weak-evidence
uncertainty, literal source injection, rejected citation originals, malformed
first attempts and correction chains, and saved results after reopening storage.

Citation membership deliberately does not certify entailment, freshness,
completeness or causality. Paired supported/contradicted claims using the same
valid citation expose that limit. Candidate support is not proof of root cause.
Actual-model behavior, live-provider workflow evidence and independent human
semantic labels are **unrun/unverified**; no model-quality result is claimed.
R01 workflow wiring and Q05 actual-output labeling remain downstream work.
