# U01 — Build one fixture-backed operator screen

**Purpose:** Make the incident, selected promises, exact draft, and run state reviewable on one operator screen before live API wiring.

- **Branch:** [`feat/operator-console`](../branches/feat-operator-console.md).
- **Owner:** P4 — frontend/evaluation owner.
- **Priority:** P0 for the promised application and its evidence.
- **Suggested git commit subject:** `feat(web): [U01] add fixture-backed operator console`.
- **Hard merge prerequisites:** [F02](F02.md).

The synthetic U01 implementation is merged into local main at
`8e151ac42e5471849e4719afd96af247a1bb36d7`. Live API wiring remains U02.
[P00](P00.md)
preserves the current monitor before foundation work. See the
[canonical commit plan](../04-commit-plan.md), [handoff contracts](../05-contracts-and-handoffs.md),
[agent reliability completion plan](../06-agent-reliability-implementation.md),
[frontend pipeline and reliability guide](../09-frontend-pipeline-and-reliability.md),
and [current monitor guide](../../../tools/monitoring/README.md).
Code may be drafted against frozen interfaces before prerequisites merge;
acceptance and live execution still wait for the listed gates.

## Current state and reuse

F01 supplies the React/HTTP scaffold and F02 supplies frozen API types. U01 now
renders synthetic examples using those contracts. The current
[monitor report](../../../src/server/evaluations/metrics.ts) remains a reference
for honest result states, not a browser API or proof of live integration.

## Allowed paths

Keep this commit within these paths from the canonical plan. Existing paths are
reused; absent paths are proposed additions. Shared-file changes belong to their
foundation owner and must land before consumers.

- `src/web/App.tsx`
- `src/web/styles.css`
- `src/web/components/IncidentInput.tsx`
- `src/web/components/StatusBand.tsx`
- `src/web/components/EvidencePanel.tsx`
- `src/web/components/PlanPanel.tsx`
- `src/web/components/RunTimeline.tsx`
- `src/web/components/EffectsPanel.tsx`
- `src/web/fixtures/demo.ts`
- `tests/app/console.test.tsx`
- `tests/e2e/console-fixtures.spec.ts`

## Sequential implementation steps

1. Freeze F02-shaped synthetic fixtures for every state in the frontend guide's
	complete state/error matrix. Include zero human labels, missing provider
	fields, claim contradictions, empty report denominators, offline/session
	errors, stale revisions, and failed partial effects. Label all examples synthetic.

2. Build IncidentInput, StatusBand, EvidencePanel, PlanPanel, and EffectsPanel in
	the guide's fixed reading order. Display selected/excluded commitments, source
	references, exact designated recipient/subject/body, effect IDs/links and
	expected/observed fields. Keep all review data read-only.

3. Build RunTimeline with the stable `ingest`, `select`, `analyze`, `draft`,
   `audit`, `approve`, `execute`, `verify`, and `assess` stage IDs. Display the
   analyst, drafter, and auditor role names only on their three model stages. Use
   declared stage/span, logical-call, model-attempt, and provider-attempt
   identities. Keep workflow, trace/process, outcome, and first-proposal
   assessments distinct; stage completion alone cannot light an overall pass.

4. Compose the one-screen layout with Slack review link/status, stable dimensions,
   and text-plus-icon labels for pending, failed and unverified states. Implement
   the guide's desktop/tablet/mobile behavior and keyboard/focus rules. Do not
   create an approval control. Keep technical detail behind an accessible
   disclosure or side panel rather than crowding the primary decision.

5. Add component/accessibility tests for exact draft rendering, completed product
   with pending assessment, wrong expected/actual recipient, original draft
   failure despite corrected-plan success, failed partial, zero-label/0-of-0,
   offline/session states and no approval bypass. Add Playwright viewport checks
   at 375x812, 768x1024, and 1440x900; attach synthetic screenshots and hand U02
   the component props and fixture contract.

## Agent, LLM and external-app integration

Use the [LLM call guide](../07-agent-spawning-and-llm-integration.md) and
[app transport guide](../08-mcp-api-and-external-app-integration.md) with the
[F02 handoff contract](../05-contracts-and-handoffs.md). This commit owns:

- The browser renders F02 fixtures for role progress, exact plan, Slack approval and verified app links; it does not instantiate agents or external-app clients. Keep model/API/MCP tokens out of web fixtures and bundles.
- Expose whether evidence is simulated or collected and show pending/failed model stages without fabricated completion. Include a Slack review link; an operator-console button is not an approval.
- Component tests check rejected/failed role state and incomplete app evidence. U02 replaces fixtures with backend routes; no browser-to-provider integration is planned.

## Do not

- Do not invent live metrics, mark fixture agent stages as executed, expose provider credentials, or add general chat.
- Do not calculate M1–M7 in components or create a UI approval authority.

## Parallel subtasks and sequential joins

- P4 owns `App.tsx`, `styles.css`, `IncidentInput.tsx`, `StatusBand.tsx`, and the
	final information hierarchy/status semantics.
- A delegated frontend helper owns `EvidencePanel.tsx` and `PlanPanel.tsx`; an
	available helper separately owns `RunTimeline.tsx` and `EffectsPanel.tsx`.
- P4's test partner owns `src/web/fixtures/demo.ts`, `tests/app/console.test.tsx`,
	and `tests/e2e/console-fixtures.spec.ts` after F02 shape agreement.

**Join:** Freeze fixtures first; independent components then join in App.tsx under P4. Run rendered-state checks after this join. U02 may be drafted concurrently against the agreed shape but merges after U01 and B04.

Names P1–P4 identify responsibility lanes, not extra people. With fewer teammates,
one person rotates through ready subtasks; never assign two simultaneous owners
to the same file. Tests and their implementation belong in this same commit.

## Verification and handoff

Attach the build/typecheck results, named component/accessibility/browser tests,
and three-viewport screenshots showing waiting, partial, and completed-but-
unverified synthetic states. Verify stable pipeline order, keyboard traversal,
focus restoration, no overlap, and explicit synthetic labeling. Hand the
component props and fixture contract to U02. No provider/demo claim is earned by
these screenshots.

Record exact commands and evidence mode. Reuse current `npm run typecheck`,
`npm run build`, `npm run test:monitor`, and `npm run test:checker` where relevant;
application-specific test commands must first be introduced by F01.
A command exit code alone is insufficient: inspect named tests, assessments,
required artifacts, and failed/unrun cases.

## Completion record

- [x] Hard prerequisite commit SHAs recorded and reviewed.
- [x] Implementation and meaningful tests land together within allowed paths plus the five reviewed foundation wiring changes below.
- [x] Required verification and consumer handoff reviewed; unrun cases disclosed.
- [x] Actual commit SHA: `8e151ac42e5471849e4719afd96af247a1bb36d7` (local main).
- [x] Evidence/report references and raw counts recorded below.
- [x] Owner: Codex root/P4; reviewer: delegated `timeline_effects`; September 14, 2026 (IST).
- [x] P1 integrator updates [Global Scale](../Global%20Scale.md) for demonstrated scope only.

## Completion receipt

Conventions **1.2**, API schema **2**, base
`7ee74a7c651a9aee9efd7928c784cf7525efbb3b`, F02 prerequisite
`87d3c7d7bad17d54300e452e932c7f226fc74299`. The user authorized merging U01.
The integrator copied exactly 16 U01 files into an isolated
`feat/operator-console` worktree, preserving concurrent B01/Q01 changes, then
fast-forwarded local main to the tested commit. No remote push was performed.

Five minimal foundation joins were reviewed: `src/web/main.tsx` mounts App and
retains the old scaffold at `/?scaffold=1`; its existing browser test uses that
URL; `vitest.config.ts` and `tsconfig.tests.json` include U01's assigned TSX test
path; `tsconfig.web.json` resolves Node declarations needed by F02's type-only
legacy exports. Shared schemas, dependencies and server code are unchanged.

Fresh isolated-candidate verification used Node **24.21.0**, prepending
`/tmp/node-v24.21.0-linux-x64/bin` to PATH:

- `npm run test:web -- tests/app/console.test.tsx tests/web/bootstrap.test.tsx`:
  **12/12 passed**, zero skips. Ten U01 tests include schema/render/no-approval
  checks for all **35 synthetic fixtures**, exact draft content, partial/unknown
  effects, wrong recipients, first-proposal failure after correction, absent
  fields, zero labels/denominators, incomplete retrieval, session states and axe.
- `npm run typecheck`: **passed**.
- `npm run build`: **passed**, server and frontend.
- `npm run test:e2e -- tests/e2e/console-fixtures.spec.ts tests/e2e/bootstrap.spec.ts`:
  **5/5 passed**, zero skips. Covers 375x812, 768x1024, 1440x900; browser axe;
  keyboard/error/reopen/disclosure focus; no horizontal overflow; wrong-recipient
  readback; no browser workflow commands. Localhost execution required an
  approved sandbox escalation.
- `git diff --cached --check`: **passed**. After fast-forward, application and
  configuration trees on main matched the tested feature commit exactly.

Nine waiting/partial/completed-but-unverified state/viewport screenshots plus a
desktop overview are generated under `.local/playwright-results/`. The original
workspace also retains selected images and detailed handoff under
`.local/u01/`. These local generated files are not tracked; the browser command
reproduces them. All images, counts, approvals and labels are synthetic. Actual
live model calls, provider workflows and human semantic reviews remain **zero**.
Monitor/checker/scenario/live suites were not rerun for this frontend-only merge.
Earlier development found and fixed a contrast issue; the final isolated
candidate had no failed or skipped checks.

U02 handoff: `ConsoleFixture` composes F02 `RunView`, optional `TraceView` and
`ApiError`; `DEMO_FIXTURES` is frozen and default ID is `awaiting_approval`.
EvidencePanel, PlanPanel, EffectsPanel and StatusBand take `{ run }`;
RunTimeline takes `{ run, trace? }`; IncidentInput accepts URL/allowlist,
disabled/error state and a submit callback. App only orchestrates local examples.
The delegated consumer review accepted these boundaries. Individual source/claim
records, retrieval timestamps, span IDs and provider-stage mapping absent from
F02 explicitly display unavailable. U02 live transport/authentication and real
provider/evidence links remain open; this merge earns no live release claim.
