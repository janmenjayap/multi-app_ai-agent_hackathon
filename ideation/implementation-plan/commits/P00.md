# P00 — Preserve the reviewed monitoring baseline

**Branch:** [`chore/monitor-baseline`](../branches/chore-monitor-baseline.md).
**Owner:** P1, with P4 reviewing the measurement receipt.
**Suggested commit subject:** `chore(P00): preserve the verified local reliability monitor`
**Hard prerequisites:** none. This is the shared starting point for F01.
**Current state:** the monitor is implemented in the working tree; a commit containing
it has not been created by this planning task. The recorded baseline is 129 tests
(95 monitor + 34 original checker), with the original checker inputs unchanged.
Consult the [receipt](../../../tools/monitoring/verification.md) and its hashes;
do not inherit the result if tested inputs have changed.

## Allowed scope and reuse

Preserve `.node-version`, `.gitignore`, `package.json`, `package-lock.json`,
`tsconfig.json`, `src/shared/reliability.ts`, `src/shared/checker.d.ts`,
`src/server/storage/monitor-store.ts`, `src/server/monitoring/`,
`src/server/evaluations/metrics.ts`, `tests/monitoring/`, and `tools/monitoring/`.
Include the explicitly reviewed documentation updates that explain this baseline.
The original `tools/reliability/check-evidence.mjs`, checker test, and synthetic
input are preserved, not rewritten.

## Sequential implementation steps

1. Inventory the working tree against its existing `main` base. Separate the
   monitor, this delivery plan, and user-owned `ideation/exploration/` relocations.
   Resolve which reviewed commit contains those relocations before publishing
   documentation that links to the new locations; never discard or recreate
   another contributor's edits to obtain a clean tree.
2. Review the monitor source, input schemas, local persistence boundaries, and
   synthetic evidence labels. Confirm no credentials, private snapshots, local
   database, `dist/`, or `node_modules/` enter the selected changes.
3. With Node 24, check the recorded input hashes. Run install, typecheck/build,
   `npm test`, and the synthetic demo for the exact reviewed source if the inputs
   changed or the receiving environment needs a fresh verification. Keep failed
   or unrun checks visible; do not replace a real result with the old 129 count.
4. Commit only the selected baseline paths and reviewed accompanying docs. Record
   the resulting real SHA and evidence in the PR and [Global Scale](../Global%20Scale.md).
   Have the integrator merge this baseline before downstream worktrees are made.
5. Verify a checkout of the merged baseline contains the monitor, lockfile, guide,
   and valid documentation links. Hand its SHA to the foundation and evaluation owners.

## Reliability baseline boundaries to preserve

- Freeze the historical `monitor-v1` and `checker-v1` source/report hashes and supplied-evidence scope. Its `falseCompletion` is premature-success-only; it cannot establish zero independently contradicted completions.
- Preserve the 129-test receipt as a recorded baseline, not a promised future count. Existing imported-mode values and generated positive human labels do not authenticate evidence or reviewers.
- Record zero executed application/model/live workflows and actual semantic reviews. The [agent reliability completion plan](../06-agent-reliability-implementation.md) schedules observation schema v2, `monitor-v2`, collector provenance, census and human labels in downstream commits; P00 must not implement them as an unreviewed baseline rewrite.

## LLM and external-app planning boundary

Preserve the [LLM integration guide](../07-agent-spawning-and-llm-integration.md)
and [MCP/API integration guide](../08-mcp-api-and-external-app-integration.md)
with the reviewed plan. P00 adds no agent spawn, model key, app token or provider
call. F01/F02 own configuration/contracts, A01–A04 own models, I01–I05 own app
transports and R01 owns live assembly. Their unrun gates remain open.

## Parallel work and sequential join

- P1 reviews/stages the source and root configuration. Only P1 edits those files.
- P4 reviews `tests/monitoring/` and the saved report independently of the implementation.
- P2 checks Node/provider access prerequisites; P3 prepares source/claim examples.
  Neither performs product workflows as part of this documentation/baseline task.
- Join the reviews before committing. Creating branches from `main` before this
  baseline merges would omit the currently uncommitted monitor.

## Do not

Do not use `git add .`, reset the dirty tree, commit generated/private files,
rewrite the preserved checker, or mark the product backend/agents as complete.
Do not push, merge, or submit anything merely because this file describes a
future step; execution is separate from writing the plan.

## Verification and handoff

Existing runnable commands are `npm run typecheck`, `npm run build`, `npm test`,
and `npm run monitor -- demo`. The demo is synthetic. Preserve the receipt and
actual code identity; [F01](F01.md) consumes this baseline and extends it.

- [ ] Baseline scope reviewed; user changes accounted for.
- [ ] Actual validation result and evidence mode recorded.
- [ ] Baseline merged; real commit SHA: **pending**.
- [ ] F01 owner has the merged base SHA and reproducible setup.
