# B02 — Select incidents and commitments with deterministic policy

**Branch:** [feat/selection-policy](../branches/feat-selection-policy.md) · **Owner:** P3.
**Suggested commit subject:** `feat(B02): enforce deterministic incident and commitment selection`
**Hard prerequisites:** [F02](F02.md), [Q01](Q01.md).
**Scope source:** [canonical B02 backlog](../04-commit-plan.md).

## Purpose and current state

**PLANNED.** Current monitoring accepts supplied manifests/events; it does not
retrieve incidents or decide eligible customers. Implement pure deterministic
selection over the complete normalized adapter input. Provider implementations
are not additional merge gates: use F02 contracts and Q01 controlled fixtures.

## Allowed paths

`src/server/policy/incident.ts`, `src/server/policy/selection.ts`,
`src/server/policy/identity.ts`;
`tests/app/selection.test.ts`. Consume Q01 fixtures read-only; request changed
business oracles from P4. No adapter, shared-schema, package, or migration edits.

## Sequential implementation

1. Parse allowlisted GitHub issue identifiers into immutable repository/issue
   identity. Reject unsupported input instead of fetching arbitrary pasted URLs.
2. Validate typed production/impact/service/environment evidence and required
   completeness metadata. Distinguish invalid/incomplete source retrieval from
   a complete valid empty eligible set.
3. Select active exact-service commitments with the frozen inclusive UTC horizon;
   resolve company, owner, and the unique explicitly designated contact by ID.
   Preserve reasons for inclusion/exclusion and block ambiguous/malformed affected rows.
4. Pin source identity and detect relevant service/environment edits without
   opening another run/effect namespace. Return typed safe block, failure, no-op,
   or selection outputs; do not perform provider writes.
5. Add Q01-backed boundary and adversarial tests in the same commit: exact
   time edges, missing owner/contact, ambiguity, closed/nonproduction input,
   failed second page, and edits to a known issue identity.

## Decision-quality evidence and safe-outcome acceptance

Reliability includes correct safe decisions as defined in the [detailed implementation plan](../06-agent-reliability-implementation.md).

1. Return the source snapshot IDs/digests, completeness receipts, frozen reference time and policy version with every selection result. Preserve excluded candidates and explicit reasons sufficiently for Q04 to compare the decision with its independent frozen oracle.
2. Do not let a source-controlled instruction, model suggestion, failed page or ambiguous designated contact alter server-owned identity policy. Complete zero-impact, safe block, source failure and eligible selection remain different outputs.
3. Make the output suitable for B04's canonical decision event without giving this pure module persistence or network authority. A complete empty selection supplies the source/completeness and selection-policy receipts needed by the typed no_affected claim; B07 still checks absence predicates. That path creates no approved plan or required final Slack artifact. B03 consumes nonempty selected IDs and source versions; it cannot replace policy failures with model confidence.
4. Add paired fixtures with identical first-page data but different page-two completeness, and with the same incident identity but changed source version. Assert the correct disposition and explanation as well as the selected IDs.

**Additional acceptance:** Q04 can reproduce every inclusion/exclusion from the frozen facts and policy version. The ambiguous-contact leg yields the expected block with no protected effects; a repaired source yields a new selection/plan revision within the same business run. These tests establish deterministic decision behavior; actual analyst/drafter quality still needs A01–A04/Q05 evidence.

## Agent, LLM and external-app integration

Use the [LLM call guide](../07-agent-spawning-and-llm-integration.md) and
[app transport guide](../08-mcp-api-and-external-app-integration.md) with the
[F02 handoff contract](../05-contracts-and-handoffs.md). This commit owns:

- R01 `workflow/nodes.ts` invokes I02 GitHub and I03 HubSpot reads, then supplies complete normalized evidence to this commit's `policy/incident.ts`, `identity.ts`, and `selection.ts`. Keep the policy functions pure; do not add a network collector or an LLM-based customer selector here.
- Return exact selected IDs and immutable evidence references for A02–A04/B03. Incomplete pages, ambiguous ownership/contact and invalid source scope stop before the first model call; complete zero-eligible selection needs zero model or mutation calls.
- Verify those paths with injected normalized API/MCP-response fixtures. Fake evidence proves policy behavior only; I02/I03 own transport completeness and live account smoke.

## Do not

- Choose fuzzy identity matches or “the first” contact, send to all contacts,
  or delegate recipient/owner choice to a model.
- Silently exclude malformed potentially affected rows or interpret missing pages
  as no customer impact.
- Add remote reads inside pure policy functions or change the frozen oracle to pass.

## Parallel work and sequential join

- **P3:** the three policy source files.
- **P4:** `tests/app/selection.test.ts`; independently label expected outcomes
  using Q01 fixtures.
- **P2:** review normalized source assumptions against F02 adapter interfaces.
- Freeze policy input/output shape before work splits. Join source implementation
  with independent boundary tests, then merge before B03 consumes selection.

## Verification and handoff

Run typecheck/build and `npm run test:app -- tests/app/selection.test.ts`.
S3 blocked leg and families 2/13/14 must produce their predeclared statuses and
zero protected dispatches in the fake harness. Provide selected IDs, exclusion
reasons, identity stability, and completeness proof to B03 and R01; these are
offline policy tests, not live provider evidence.

## Completion receipt

- [ ] F02/Q01 merged; prerequisite SHAs: `UNRECORDED`.
- [ ] Selection implementation and independent boundary tests committed together.
- [ ] Fixture/policy version, commands, pass/fail/unrun evidence: `UNRECORDED`.
- [ ] P4/P2 review and B03 acceptance: `UNRECORDED`.
- [ ] Actual B02 commit SHA: `UNRECORDED`.
