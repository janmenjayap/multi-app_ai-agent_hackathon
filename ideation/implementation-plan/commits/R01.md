# R01 — Assemble the guarded graph and prove the first vertical slice

**Purpose:** Join the separate modules into one deterministic application path and establish actual S1/S2 evidence.

- **Branch:** [`feat/workflow-integration`](../branches/feat-workflow-integration.md).
- **Owner:** P1 — sole graph/composition integrator.
- **Priority:** P0 for the promised application and its evidence.
- **Suggested git commit subject:** `feat(workflow): [R01] compose guarded four-app execution`.
- **Hard merge prerequisites:** [B02](B02.md), [B03](B03.md), [B04](B04.md), [B05](B05.md), [B06](B06.md), [B07](B07.md), [I02](I02.md), [I03](I03.md), [I04](I04.md), [I05](I05.md), [A02](A02.md), [A03](A03.md), [A04](A04.md), [Q01](Q01.md), [Q02](Q02.md), [Q03](Q03.md).

The guarded implementation and simulated evidence are recorded below; live and
measured-release gates remain open. [P00](P00.md)
preserves the current monitor before foundation work. See the
[canonical commit plan](../04-commit-plan.md), [handoff contracts](../05-contracts-and-handoffs.md),
[agent reliability completion plan](../06-agent-reliability-implementation.md),
[frontend pipeline and reliability guide](../09-frontend-pipeline-and-reliability.md),
and [current monitor guide](../../../tools/monitoring/README.md).
Code may be drafted against frozen interfaces before prerequisites merge;
acceptance and live execution still wait for the listed gates.

## Current state and reuse

Only the offline checker and standalone monitor execute today; no HTTP app, graph, agents, provider adapters, runtime verifier, or scenario harness exists. Reuse the monitor through B01/Q03's same-transaction application integration; prewritten synthetic events are not this vertical slice.

## Allowed paths

Keep this commit within these paths from the canonical plan. Existing paths are
reused; absent paths are proposed additions. Shared-file changes belong to their
foundation owner and must land before consumers.

- `src/server/workflow/graph.ts`
- `src/server/workflow/nodes.ts`
- `src/server/composition.ts`
- `src/server/index.ts`
- `tools/demo/run.ts`
- `tests/scenarios/walking-skeleton.test.ts`

## Sequential implementation steps

1. Review the prerequisite SHAs and frozen F02/Q01 contracts before composition. Keep P1 as sole graph owner. Instantiate restricted model/provider capabilities outside serialized graph state, the B01 business ledger, Q03 local assessor, and the separately controlled Q02 read-only collector.

2. Branch complete zero-eligible selection to `completed_no_affected_commitments` with `no_affected` source/selection/no-write evidence; it requires no plan, approval or Slack artifact. For eligible work, compose complete selection → analyst → drafter → deterministic guards → blind auditor → immutable exact plan → Slack review/readback → pure approval interrupt. Persist original model inputs/outputs or failures and source digests before retries/corrections. Resume reloads authentic approval, source freshness and the approved plan.

3. Compose B06 ordered HubSpot task/note, Gmail draft, and GitHub comment through B07 readback. Slack coordination uses its narrow deterministic path; read back the final summary before terminal success. Record every stage/span/logical tool call, provider attempt and success claim with stable IDs. Every protected dispatch rechecks approval/hash/freshness.

4. Use the single B01 transaction for state/event/measurement scheduling. Wire Q03's v2 assessor and Q02's provenance-bearing bundles at this integration join; Q03's earlier unit tests with an optional collector interface do not establish live provider origin. Missing collector, label or timing evidence remains unverified.

5. Add walking-skeleton tests and `tools/demo/run.ts` using the real graph and independent stateful provider reads. The demo tool performs mandatory preflight/S0 under `suiteEntryId`, registers only after successful setup and binds the S0 hash immediately before graph dispatch. It also provides minimal trusted human-review receipt input through F02/B01, binding actual reviewer identity/reason to original source/output digests before quality credit. This supports the first vertical slice without a Q05 prerequisite; synthetic human-label JSON cannot establish actual review. Cover complete S1, replay, wait/restart, safe block, stale source, accepted unknown write, monitor outage, wrong recipient, absent draft and phantom Slack success. Intentionally wrong tool acknowledgements must be caught by the runtime verifier and evaluator.

6. After simulated graph checks pass, execute scoped live S1 then S2 with frozen expectations and separate S0/S1 collection. Bind generated exact bytes through B03's immutable `ApprovedContentRef` before dispatch; preserve the independent pre-run semantic oracle. Preserve all five artifact IDs/fields, authentic approval and original-output review receipts; prove replay adds no effects. Record failed and unverified legs as well as successes.

7. Project the real `ingest`, `select`, `analyze`, `draft`, `audit`, `approve`,
   `execute`, `verify`, and `assess` stages through B04/F02 without changing their
   stable IDs. Label only the analyze/draft/audit stages with analyst/drafter/
   auditor roles; never call deterministic controls agents. Include actual model/
   provider attempt children, effects/readbacks, plan/approval, assessment
   availability and redacted links. Test that a successful graph exit cannot
   overwrite pending or failed assessment with a green browser state.

8. Hand Q04 the graph invocation/fault seams plus collector/assessment references,
   and U02/R02 saved walking-skeleton projections for completed, awaiting approval,
   safe block, failed partial, replay and completed-but-unverified states. R01 can
   prove the first vertical slice without requiring U02 or Q04/Q05 to have merged;
   it must not claim a working browser, full suite or final cohort report.

## Agent, LLM and external-app integration

Use the [LLM call guide](../07-agent-spawning-and-llm-integration.md) and
[app transport guide](../08-mcp-api-and-external-app-integration.md) with the
[F02 handoff contract](../05-contracts-and-handoffs.md). This commit owns:

- In `src/server/composition.ts`, create B01 repositories, the A01 model client/runtime and the four I01–I05 app clients from validated F01 config. Inject only role-specific dependencies and separate B07/Q02 readers. `src/server/index.ts` owns startup/shutdown; no credentials or live clients enter graph state.
- In `workflow/nodes.ts`, implement GitHub/HubSpot source reads -> B02 selection -> A02 `analyzeIncident` -> A03 `draftCustomerUpdate` -> B03 deterministic proposal validation -> A04 `auditSemantics` -> B03 plan freeze. `workflow/graph.ts` enforces that order; every role calls A01 `invokeRole` -> `model.ts` -> OpenAI. This is the project's agent-spawning integration point.
- Wire B05 Slack review/read/decision wait, B06 HubSpot task/note -> Gmail draft -> GitHub comment, B07 per-effect readback -> Slack summary/readback, and independently invoked Q02 readers. All REST methods and the optional MCP boundary are listed in guide 08; models receive no tools.
- `tools/demo/run.ts` and the walking-skeleton test prove the call chain, role-context isolation, mode/auth failures, unchanged approval-resume call counts and separate provider observations. Live OpenAI with fake apps proves model integration only. Live model + four live REST adapters, authentic Slack approval and independent reads are required for the live vertical slice.
- The walking-skeleton test also validates that real graph state serializes through
	the frozen public `RunView`/event contracts with stable revisions and no secret,
	raw private artifact, or model/provider client leakage. This is API projection
	evidence for U02, not proof that U02 has rendered it.

## Do not

- Do not create a second orchestration loop, pass mutation tools to models, run side effects inside the pure interrupt node, or infer completion from graph progression.
- Do not bypass approval/collector gates to obtain a demo, use new keys for revised effects, or claim synthetic fixtures are live S1/S2. Do not change teammates' module files inside the composition PR.

## Parallel subtasks and sequential joins

- P1 exclusively owns `graph.ts`, `nodes.ts`, `composition.ts`, and `src/server/index.ts`.
- P4 owns `tests/scenarios/walking-skeleton.test.ts`, independent expected evidence,
  and saved redacted projection assertions; P3 reviews role projections and source
  grounding without changing composition.
- P2 owns `tools/demo/run.ts` on this branch and prepares scoped test-account access; actual live runs join only after guards and collection are ready.

**Join:** Node wiring may be drafted against F02/Q01 fixtures while upstream code is built, but all listed prerequisites must merge before integration acceptance. Runtime analyst/drafter/auditor and protected effects remain sequential. P1 joins code; P4 collects evidence independently.

Names P1–P4 identify responsibility lanes, not extra people. With fewer teammates,
one person rotates through ready subtasks; never assign two simultaneous owners
to the same file. Tests and their implementation belong in this same commit.

## Verification and handoff

Attach real graph tests and separate simulated/live evidence. Live S1 requires all five artifacts across four apps, exact approved draft, protected Beta, and valid decision/source history. S2 preserves IDs with zero excess creates. Include F02-valid redacted run/event projections for U02/R02 and prove their stage/effect identities match stored events. Failed/unrun live proof leaves the working-product gate open. Hand the graph invocation seam to Q04 and API projection evidence, not screenshots, to U02/R02.

Record exact commands and evidence mode. Reuse current `npm run typecheck`,
`npm run build`, `npm run test:monitor`, and `npm run test:checker` where relevant;
application-specific test commands must first be introduced by F01.
A command exit code alone is insufficient: inspect named tests, assessments,
required artifacts, and failed/unrun cases.

## Completion record

- [x] Hard prerequisite commit SHAs recorded and reviewed.
- [x] Implementation and focused tests land together; reviewed integration exceptions listed below.
- [x] Simulated verification and consumer handoff reviewed; live and oracle-binding gaps disclosed.
- [x] Actual implementation commit SHA: `bacb8ea4e2c340f547fe2a081c6836b911fe24d3`.
- [x] Evidence/report references and raw counts: tests/scenarios/walking-skeleton.test.ts, tools/demo/run.ts, and receipt below; frozen-oracle result remains unverified.
- [x] Owner/reviewers: Codex P1 integrator and delegated composition, execution, and scenario lanes; September 14, 2026.
- [x] P1 updates [Global Scale](../Global%20Scale.md) for demonstrated scope only.


## R01 implementation receipt — September 14, 2026

Conventions **1.2**. Owner: Codex P1 integrator; delegated Codex composition,
execution/readback, and walking-skeleton reviewers. These are agent code reviews,
not human semantic labels. Base: `b4ce714d6d3684926dfe76175bb43cfe5185f455`.

All hard prerequisite implementation commits are ancestors of this base:

- B02: `a7a0fcb4d3b893595d1dc33c8afcf96926bc5cb6`.
- B03: `fe017b511b28cc8cef76c46d706019f3b6f56753`.
- B04: `7b13aa0b15c558526bfe9a25c15cf1649f101f14`.
- B05: `c6b8dcde61d7e997a22918730be2f396556a8268`.
- B06: `192a1db0ff50118a33c8d7d5dab3c2ec3ed964b7`.
- B07: `fcbe5db60cc39cdc3ed45519848b8251e2f678ab`.
- I02: `2fdb5ac55ea24687f04f83c064587dd939274fd6`.
- I03: `558e927fb33c5ae48312a0f984fa6e7da60e3f21`.
- I04: `ea054ab73f95de3f219304e218b12237d119e175`.
- I05: `dc1733fb947c95a9d0ca24c0a85b45d99e87c75a`.
- A02: `ffe2ccaf9911dc0f8eb9df0142e7fe6cfd973e3a`.
- A03: `afa83ab7053f6baad8ad960ff626304c3aa57492`.
- A04: `784b32bf96b26ab96188eef6ca9596fe3cdeb6c3`.
- Q01: `bcced55d61b90b2ce69e077ecc0475b814e27335`.
- Q02: `81c1b84d8a6114dca1afc320817b6ad132d7a808`.
- Q03: `df9e0102d8b6e932138a96dad5ece64813669dea`.

The application now composes source reads, deterministic selection, three recorded
roles, proposal guards, immutable plan freeze, authentic-reader Slack approval,
ordered protected writes, independent inline readback, final Slack verification,
and assessment through the existing B04 driver. Empty selection performs fresh
source/absence checks and skips roles, approval, and writes. Resume reloads the
ledger; clients and credentials remain outside graph state.

The runnable demo is explicitly simulated. It uses stateful providers, independent
reader calls, and the real A01/B04/B05/B06/B07 pipeline. Original model bytes and
source references remain available; generated replies never earn human-review
credit. The optional interactive review entry requires an actual terminal
interaction and binds the reviewer to original source/output digests.

Necessary prerequisite integration corrections, reviewed by the owning delegated
lanes, extend the original allowed-path list:

- `policy/plan.ts`: freeze provider-compatible GitHub markers and deterministic
  artifact references in GitHub/Slack bodies before approval.
- `verification/readback.ts` and `storage/repositories.ts`: permit a fresh
  re-verification of verified effects while retaining append-only evidence;
  preserve the execute stage for inline readback.
- `workflow/driver.ts`: reserve server-owned run/evaluation/runtime IDs before
  preflight so S0 registration and subsequent collection share identity.
- `tests/fakes/providers.ts`: use Slack timestamp identities and fresh observation
  timestamps matching the real reader contract.
- Existing `tests/app/verification.test.ts` expects scoped Slack thread reads.
- Test/demo TypeScript discovery and emission configuration; no new dependency.
- README/environment documentation for the executable demo and trusted startup.

Evidence limits remain explicit: live OpenAI/four-provider S1/S2 and actual human
quality review were not run. Q01's frozen oracle generator differs from B03 in
effect-key policy, subject/content references, and source wrapping. R01 retains
the frozen oracle and reports this binding gap as unverified; simulated runtime
completion does not close the measured-release or live-product gate. Q04/Q05
receive the actual graph invocation/fault seams and this incompatibility for a
reviewed evaluator binding change. No full cohort report or browser proof is claimed.

Verification uses Node 24.21.0 and the existing pinned dependencies.

- `npm run test:app -- tests/scenarios/walking-skeleton.test.ts`: **3/3 passed**;
  seven simulated scenario setups grouped around success/replay, blocks/absence,
  and wrong-recipient/phantom-summary/accepted-unknown-write behavior.
- Existing plan and readback tests: **18/18 passed** during integration; after
  scoped Slack lookup adjustment, readback tests: **10/10 passed**.
- Four selected existing driver checks passed for concurrency, wait/restart,
  no-affected completion, and interrupted execution.
- `npm run typecheck` and `npm run build`: passed.
- Standalone demo uses `npm exec -- tsc -p tools/demo/tsconfig.json` then
  `node .local/demo-build/tools/demo/run.js`; it performs simulated S1, restart,
  and S2, preserving three model calls and five verified effects.
- Full regression/browser/live-provider suites were deliberately not run under
  the user's minimal-testing instruction. No live or human-review success is claimed.

The initial integration checks exposed the cross-module mismatches described
above; those runtime failures were fixed before the passing receipts. The frozen
oracle/export mismatch remains visible and is not converted into a pass.

Consumer handoff: use `createWorkflowDefinition` with `createComposition`;
`createDemoHarness` supplies reusable isolated providers, fault controls, S0/S1
collection, and registered execution. Tests assert F02-valid projections against
persisted stage/effect identities. Runtime authentication and dataset policy are
server-owned inputs through `createCompositionOptions(config)` for production
startup. The runnable simulated demo requires no provider credentials.
