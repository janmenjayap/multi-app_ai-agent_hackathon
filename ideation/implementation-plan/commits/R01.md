# R01 — Assemble the guarded graph and prove the first vertical slice

**Purpose:** Join the separate modules into one deterministic application path and establish actual S1/S2 evidence.

- **Branch:** [`feat/workflow-integration`](../branches/feat-workflow-integration.md).
- **Owner:** P1 — sole graph/composition integrator.
- **Priority:** P0 for the promised application and its evidence.
- **Suggested git commit subject:** `feat(workflow): [R01] compose guarded four-app execution`.
- **Hard merge prerequisites:** [B02](B02.md), [B03](B03.md), [B04](B04.md), [B05](B05.md), [B06](B06.md), [B07](B07.md), [I02](I02.md), [I03](I03.md), [I04](I04.md), [I05](I05.md), [A02](A02.md), [A03](A03.md), [A04](A04.md), [Q01](Q01.md), [Q02](Q02.md), [Q03](Q03.md).

This is a future implementation task, not a completed commit. [P00](P00.md)
preserves the current monitor before foundation work. See the
[canonical commit plan](../04-commit-plan.md), [handoff contracts](../05-contracts-and-handoffs.md),
[agent reliability completion plan](../06-agent-reliability-implementation.md),
and [current monitor guide](../../../tools/monitoring/README.md).
Code may be drafted against frozen interfaces before prerequisites merge;
acceptance and live execution still wait for the listed gates.

## Current state and reuse

Only the offline checker and standalone monitor execute today; no HTTP app, graph, agents, provider adapters, runtime verifier, or scenario harness exists. Reuse the monitor through B01/Q03's same-transaction application integration; prewritten synthetic events are not this vertical slice.

## Allowed paths

Keep this commit within these paths from the canonical plan. Existing paths are
reused; absent paths are proposed additions. Shared-file changes belong to their
foundation owner and must land before consumers.

- `src/server/workflow/graph.ts`
- `src/server/workflow/nodes.ts`
- `src/server/composition.ts`
- `src/server/index.ts`
- `tools/demo/run.ts`
- `tests/scenarios/walking-skeleton.test.ts`

## Sequential implementation steps

1. Review the prerequisite SHAs and frozen F02/Q01 contracts before composition. Keep P1 as sole graph owner. Instantiate restricted model/provider capabilities outside serialized graph state, the B01 business ledger, Q03 local assessor, and the separately controlled Q02 read-only collector.

2. Branch complete zero-eligible selection to `completed_no_affected_commitments` with `no_affected` source/selection/no-write evidence; it requires no plan, approval or Slack artifact. For eligible work, compose complete selection → analyst → drafter → deterministic guards → blind auditor → immutable exact plan → Slack review/readback → pure approval interrupt. Persist original model inputs/outputs or failures and source digests before retries/corrections. Resume reloads authentic approval, source freshness and the approved plan.

3. Compose B06 ordered HubSpot task/note, Gmail draft, and GitHub comment through B07 readback. Slack coordination uses its narrow deterministic path; read back the final summary before terminal success. Record every stage/span/logical tool call, provider attempt and success claim with stable IDs. Every protected dispatch rechecks approval/hash/freshness.

4. Use the single B01 transaction for state/event/measurement scheduling. Wire Q03's v2 assessor and Q02's provenance-bearing bundles at this integration join; Q03's earlier unit tests with an optional collector interface do not establish live provider origin. Missing collector, label or timing evidence remains unverified.

5. Add walking-skeleton tests and `tools/demo/run.ts` using the real graph and independent stateful provider reads. The demo tool performs mandatory preflight/S0 under `suiteEntryId`, registers only after successful setup and binds the S0 hash immediately before graph dispatch. It also provides minimal trusted human-review receipt input through F02/B01, binding actual reviewer identity/reason to original source/output digests before quality credit. This supports the first vertical slice without a Q05 prerequisite; synthetic human-label JSON cannot establish actual review. Cover complete S1, replay, wait/restart, safe block, stale source, accepted unknown write, monitor outage, wrong recipient, absent draft and phantom Slack success. Intentionally wrong tool acknowledgements must be caught by the runtime verifier and evaluator.

6. After simulated graph checks pass, execute scoped live S1 then S2 with frozen expectations and separate S0/S1 collection. Bind generated exact bytes through B03's immutable `ApprovedContentRef` before dispatch; preserve the independent pre-run semantic oracle. Preserve all five artifact IDs/fields, authentic approval and original-output review receipts; prove replay adds no effects. Record failed and unverified legs as well as successes.

7. Hand Q04 the graph invocation/fault seams plus collector/assessment references, and U02 the persisted status/report contract. R01 can prove the first vertical slice without requiring Q04/Q05 to have merged; it must not claim the full suite or final cohort report.

## Agent, LLM and external-app integration

Use the [LLM call guide](../07-agent-spawning-and-llm-integration.md) and
[app transport guide](../08-mcp-api-and-external-app-integration.md) with the
[F02 handoff contract](../05-contracts-and-handoffs.md). This commit owns:

- In `src/server/composition.ts`, create B01 repositories, the A01 model client/runtime and the four I01–I05 app clients from validated F01 config. Inject only role-specific dependencies and separate B07/Q02 readers. `src/server/index.ts` owns startup/shutdown; no credentials or live clients enter graph state.
- In `workflow/nodes.ts`, implement GitHub/HubSpot source reads -> B02 selection -> A02 `analyzeIncident` -> A03 `draftCustomerUpdate` -> B03 deterministic proposal validation -> A04 `auditSemantics` -> B03 plan freeze. `workflow/graph.ts` enforces that order; every role calls A01 `invokeRole` -> `model.ts` -> OpenAI. This is the project's agent-spawning integration point.
- Wire B05 Slack review/read/decision wait, B06 HubSpot task/note -> Gmail draft -> GitHub comment, B07 per-effect readback -> Slack summary/readback, and independently invoked Q02 readers. All REST methods and the optional MCP boundary are listed in guide 08; models receive no tools.
- `tools/demo/run.ts` and the walking-skeleton test prove the call chain, role-context isolation, mode/auth failures, unchanged approval-resume call counts and separate provider observations. Live OpenAI with fake apps proves model integration only. Live model + four live REST adapters, authentic Slack approval and independent reads are required for the live vertical slice.

## Do not

- Do not create a second orchestration loop, pass mutation tools to models, run side effects inside the pure interrupt node, or infer completion from graph progression.
- Do not bypass approval/collector gates to obtain a demo, use new keys for revised effects, or claim synthetic fixtures are live S1/S2. Do not change teammates' module files inside the composition PR.

## Parallel subtasks and sequential joins

- P1 exclusively owns `graph.ts`, `nodes.ts`, `composition.ts`, and `src/server/index.ts`.
- P4 owns `tests/scenarios/walking-skeleton.test.ts` and independent expected evidence; P3 reviews role projections and source grounding without changing composition.
- P2 owns `tools/demo/run.ts` on this branch and prepares scoped test-account access; actual live runs join only after guards and collection are ready.

**Join:** Node wiring may be drafted against F02/Q01 fixtures while upstream code is built, but all listed prerequisites must merge before integration acceptance. Runtime analyst/drafter/auditor and protected effects remain sequential. P1 joins code; P4 collects evidence independently.

Names P1–P4 identify responsibility lanes, not extra people. With fewer teammates,
one person rotates through ready subtasks; never assign two simultaneous owners
to the same file. Tests and their implementation belong in this same commit.

## Verification and handoff

Attach real graph tests and separate simulated/live evidence. Live S1 requires all five artifacts across four apps, exact approved draft, protected Beta, and valid decision/source history. S2 preserves IDs with zero excess creates. Failed/unrun live proof leaves the working-product gate open. Hand the graph invocation seam to Q04 and UI evidence to R02.

Record exact commands and evidence mode. Reuse current `npm run typecheck`,
`npm run build`, `npm run test:monitor`, and `npm run test:checker` where relevant;
application-specific test commands must first be introduced by F01.
A command exit code alone is insufficient: inspect named tests, assessments,
required artifacts, and failed/unrun cases.

## Completion record

- [ ] Hard prerequisite commit SHAs recorded and reviewed.
- [ ] Implementation and meaningful tests land together within allowed paths.
- [ ] Required verification and consumer handoff reviewed; unrun cases disclosed.
- [ ] Actual commit SHA: **pending**.
- [ ] Evidence/report references and raw counts: **pending**.
- [ ] Owner, reviewer, and verification date: **pending**.
- [ ] P1 updates [Global Scale](../Global%20Scale.md) for demonstrated scope only.
