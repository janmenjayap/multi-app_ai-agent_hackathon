# A03 — Add the Customer Update Drafter

**Purpose:** Generate grounded customer text from a fixed input scope while leaving recipient, owner, dates, and action authority in deterministic code.

- **Branch / owner:** [`feat/agent-drafter`](../branches/feat-agent-drafter.md) / P3.
- **Hard merge prerequisites:** [A01](A01.md). Preserve the [source backlog](../04-commit-plan.md)'s gates; early fixture work does not waive them.
- **Suggested commit subject:** `feat(agents): add grounded customer update drafter [A03]`
- **Priority:** P0. Implemented and merged; see the [completion receipt](#completion-receipt).

## Planning baseline and reuse

No drafter implementation exists. Use A01, F02 assessment fixtures, and the [existing first-proposal measurement contract](../../../tools/monitoring/README.md). A02 need not merge before this branch develops, but real workflow drafting must consume a validated analyst result.

The app/agent/provider portions of F02 and the product runtime remain unfinished.
Use Q01 fixtures when available through their owner; Q01 is not an added hard merge
gate for this ID. No live provider or model call is claimed by this document.

## Allowed paths

- `src/server/agents/drafter/index.ts`
- `src/server/agents/drafter/prompt.ts`
- `src/server/agents/drafter/validate.ts`
- `tests/app/drafter.test.ts`

Only the listed paths belong to this commit. Ask the owning branch for a shared
interface change before integrating dependent code; do not fork that interface.

## Sequential implementation steps

1. Implement prompt.ts with validated assessment, original supporting facts, and exactly selected commitments. State uncertainty and permitted next steps without introducing identity-selection or mutation authority.
2. Implement validate.ts for required content, claim references, unsupported dates/certainty, and attempts to change server-owned recipient/owner/action fields. Coordinate expected fields with B03 through the frozen contract.
3. Implement index.ts through A01, returning customer text and claim-to-source references with immutable first-draft evidence. Keep body generation separate from future destination IDs and approval execution.
4. Add independently labeled valid, incomplete, contradictory, changed-identity, invented-ETA, and injection fixtures in drafter.test.ts. Exercise the module using an F02 analyst fixture so A02 development can proceed independently.
5. Run targeted tests and F01 build/typecheck. When real outputs exist, review original drafts before repairs, retain correction records, and hand the draft contract to R01 and the evidence references to Q05.

## Exact drafter invocation and Gmail boundary

Follow [agent/LLM integration](../07-agent-spawning-and-llm-integration.md).
Export `draftCustomerUpdate(input: DrafterInput, ctx: AgentInvocationContext, deps)`
from **drafter/index.ts**, returning `AgentCallResult<DraftProposal>` through F02
schemas. R01's **src/server/workflow/nodes.ts** calls it in
`draft_customer_update` after a validated `IncidentAssessment` exists. Its
upstream artifact is passed as data; the drafter does not import/call A02 itself.

**The actual request path is** graph node → `draftCustomerUpdate` → A01
`invokeRole` → A01 `dispatchStructured` → `ChatOpenAI`/OpenAI Responses.
**drafter/prompt.ts** builds a fresh context from validated assessment, original
source facts, exact selected commitment facts and permitted next steps;
**drafter/validate.ts** checks claim references and attempted server-field changes
after A01 has saved the raw response. Return exact text and immutable input/output
references. B03's proposal checks run first; the auditor then receives a separately
built original-evidence/text projection. Only after a clear audit does B03 freeze
the accepted bytes into the immutable review plan.

Use `role=drafter` with the stable run/revision/role key and immutable upstream
assessment/source digest. A01 resolves optional `OPENAI_MODEL_DRAFTER` and reuses
matching committed results. No env reading, direct OpenAI import, independent
retry loop, or model regeneration while awaiting approval belongs here. One
logical invocation returns bounded draft entries keyed by exactly all selected
commitment IDs; the seeded demo happens to contain one entry. F02 freezes the
set schema and unique-entry/cardinality checks. Reject extra, duplicate, or
missing commitment IDs, and block an oversized set instead of dropping records
or introducing a per-customer spawn loop. B03 freezes each selected draft's bytes.

This role generates content only. **Gmail draft creation occurs later in B06
through I05 after exact approval**, with B07 independent readback; HubSpot tasks,
notes, GitHub comments and Slack coordination likewise use the deterministic
[external-app integration path](../08-mcp-api-and-external-app-integration.md).
Do not give the drafter provider clients, MCP tools, recipient-selection power,
or Gmail send authority. A model-produced sentence cannot establish approval.

**Additional invocation acceptance:** inspect the injected A01 request for fixed
scope and original-source references; reject invented dates, unsupported certainty,
extra recipient/owner fields and invalid citation IDs without losing raw text.
Test multiple selected commitments with exactly one returned draft per selected
ID, and invalid missing/duplicate/extra entries in the same single invocation.
R01 proves analyst → drafter ordering, zero extra drafter calls on Slack resume,
and that deterministic proposal rejection prevents auditor/plan progression.

## First draft, corrections, and semantic evidence

Implement the [reliability plan](../06-agent-reliability-implementation.md)'s customer-text evidence path.

1. Preserve the exact original draft and every claim-to-source reference, selected commitment IDs, source/analyst versions and A01 output digest before validation or edits. Source text and the validated analyst result remain independently available for review.
2. Produce a reviewable required-content checklist covering impact, uncertainty, agreed commitment context and honest next steps. This is an input to independent labeling, not a model-created pass verdict.
3. Keep human/model corrections as distinct revisions with reasons and superseded-output links. B03 freezes the approved revision; Q05 still scores the first draft. Neither Slack approval nor a successful Gmail create changes M7's first-output denominator/result.
4. Extend fixtures with a valid citation attached to an invented ETA, unsupported certainty, omitted uncertainty, incorrect customer scope and a later corrected success. Assert that fixed recipients/owners and original bad output survive every path.

**Additional acceptance:** B03 receives exact bytes plus immutable source/output references, and Q05 can review the uncorrected text without reconstructing it from the final draft. Real draft grounding/completeness remains unverified until actual human labels with source/output digests and reasons are recorded.

## Do not

- Do not choose recipients, owners, or action types; widen affected customers; invent recovery dates; or rewrite approved content after plan freeze.
- Do not create Gmail drafts, send email, import A02's implementation, or claim approval from the auditor's output.
- Do not modify B03 policy files, F02 schemas, A01 runtime, packages, storage, or graph composition.

## Parallel subtasks and sequential joins

- P3 owns drafter/index.ts and drafter/prompt.ts; a named role delegate may own drafter/validate.ts.
- P4 owns tests/app/drafter.test.ts and source-based expected labels.
- A02/A04 remain separate peer branches; the B03 owner reviews server-owned field expectations without concurrent edits to this role's files.

**Join:** Agree fixed scope and server-owned fields → parallel prompt, validation, and labeled fixtures → integrate A01 call → tests → R01 binds actual A02 output sequentially.

Assign delegates only when capacity exists; one person owns each listed file.
Development parallelism does not alter the runtime agent order or serial guarded
HubSpot task → note → Gmail draft → GitHub comment → Slack finalization.

## Verification and handoff

Drafts contain supported required context, uncertainty, and honest handoff; counterexamples expose unsupported statements and identity changes. Preserve first drafts and human/model corrections separately. Actual model quality stays unverified until real outputs receive independent labels.

Module conformance can be reviewed with deterministic fixtures. Q05's independently
labeled actual-model evidence is a separate quality claim, not an added hard merge
prerequisite for this module.

After F01 provides the proposed application runner, use
`npm run test:app -- tests/app/drafter.test.ts`, followed by the agreed typecheck/build gates.
That application-test command is planned, not currently installed by the standalone
monitor. Preserve relevant existing monitor/checker regressions when the F02 event
seam is exercised. Report fixture, model, and live-provider evidence separately.

**Consumers:** [R01](R01.md), [Q05](Q05.md). Include the normalized interface/artifact, exact
commands and exit codes, fixture/model/policy versions as relevant, and failed or
unrun gates. Store private IDs/raw evidence outside committed public examples.

## Completion receipt

Implemented September 14, 2026 under conventions **1.2**.

- Implementation: `afa83ab7053f6baad8ad960ff626304c3aa57492`.
- Original base: `e27ee70ce85143a58ae73ce604afcf4be76f12a6`.
- Hard prerequisite A01: `3923ed30c232a1b722b75c9343859c6487b3ad9a`.
- Main merge: `6af6d75f81a4482bf817da05e686639a8dfe6c20`.
- Implementation diff: only the four allowed source/test paths above. This
  separate documentation receipt updates the brief, branch handoff and C06 status.
- Versions: shared schema 2, `drafter-prompt-v1`, `drafter-v2`,
  `drafter-validation-v1`; fixture `a03-test-v1`; Node **24.21.0**.

Verification uses `PATH=/tmp/node-v24.21.0-linux-x64/bin:$PATH` in this environment:

- `npm run test:app -- tests/app/drafter.test.ts`: **17/17 passed**, exit 0.
- `npm run typecheck`: passed, exit 0.
- `npm run build`: server and web passed, exit 0.
- `git diff --check`: passed. No root/shared/storage/event changes belong to A03;
  broad monitor/checker and browser suites were not rerun for this module.
- Earlier fixture-only failures exposed missing stage/correction events and
  reused spans across revisions; the test harness was corrected before the
  passing receipt. Independent review found an unassessed-source quotation
  bypass; the validator fix and two regression cases are included.
- Model mode **mock**, evidence **synthetic_fixture**, zero provider requests.
  Live model smoke, live provider execution and actual human semantic labels
  remain **unrun/unverified**; they are not inferred from fixture success.

Independent Codex reviewer `a03_review` accepted module conformance and the
R01/Q05 handoffs after re-review. This is agent code review, not human quality
labeling. The integrating Codex owner records this receipt and C06 status.

R01 calls `draftCustomerUpdate(DraftInput, ctx, deps)` using F02's executable
`DraftInput` name and `createDrafterDependencies` to bind A01. It supplies the
validated analyst projection and complete deterministic selection; no A02 or
provider implementation is imported here. One call covers the exact set, and
replay returns saved results. A01 saves the original input, assessment, sources,
reviewable required-content checklist and raw output with immutable references.
B03 receives unchanged output bytes; graph ordering and plan freeze remain R01/B03.

Q05 reads A01 original-output records and the frozen input artifact. Format
repairs retain the first output and previous-output links. Semantic rejection
does not trigger regeneration; R01 records any later revision through B01's
`CorrectionArtifact`, retaining first-output identity, reason and superseded link.
The focused test demonstrates that handoff without relabeling the bad original.

The validator deliberately accepts complete source-fact quotations and exact
required checklist content, rejecting free factual paraphrases and unassessed
source claims. This conservative prototype gate preserves qualifications but
does not prove source truth or actual customer prose quality. Independent semantic
review remains required. `tmp.md` was neither staged nor committed.
