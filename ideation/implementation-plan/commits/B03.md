# B03 — Freeze immutable plans, claims, and effect identities

**Branch:** [feat/plan-policy](../branches/feat-plan-policy.md) · **Owner:** P3.
**Suggested commit subject:** `feat(B03): freeze exact plans and stable effect identities`
**Hard prerequisite:** [B02](B02.md).
**Scope source:** [canonical B03 backlog](../04-commit-plan.md).

## Purpose and current state

**IMPLEMENTED / VERIFIED OFFLINE.** B03 now constructs and verifies immutable
application plans, normalized approved content, role/claim provenance receipts,
stable logical effect keys, success-claim scope templates, and declared future-ID
substitution bindings. It reuses the frozen F02 canonical and plan contracts and
adds no model, network, persistence, approval, or provider authority.

## Allowed paths

`src/server/policy/claims.ts`, `src/server/policy/plan.ts`,
`src/server/policy/canonical.ts`, `src/server/policy/effect-keys.ts`;
`tests/app/plan.test.ts`.
Consume frozen F02 role/selection types and injected role fixtures. Shared utility
changes require a P1 foundation follow-up, not a competing canonicalizer.

## Sequential implementation

1. Map validated selection and scoped role outputs into exact server-owned
   recipient, owner, due date, action types, and claim/citation requirements.
   Validate references mechanically; retain semantic review as a separate requirement.
2. Define approved-text byte normalization and permitted future-ID substitution
   templates once. Include all source versions, selection, effects, and exact
   content in the canonical immutable plan hash.
3. Derive stable effect keys from pinned incident, app, stable business target,
   and action type. Keep plan revision, retry attempt, and destination-created
   IDs out of logical effect identity.
4. Preserve first proposal references and return immutable versioned plans;
   changing a relevant field creates a new revision/hash without rewriting the
   original proposal or previously applied effects.
5. Add hash vectors and adversarial plan tests in this commit: equivalent key
   order, meaningful array/Unicode/line-ending differences, changed owner/body/
   source, multi-recipient To, any Cc/Bcc, and stable keys across revisions.

## Frozen expectations, claims, and approval evidence

Apply the [reliability plan](../06-agent-reliability-implementation.md)'s distinction between approved content and independently evaluated truth.

1. Freeze the first analyst/drafter output references and source digests alongside the final proposal revision. Human or model correction creates a separate revision with reason; it cannot erase the original result used by M7.
2. Record each permitted artifact/content obligation and exact scope. Q01 freezes source facts and semantic invariants before the model call; it cannot predict exact generated wording. Resolve ApprovedContentRef only to exact normalized bytes/digests in this immutable approved plan before dispatch. Provider-created IDs use the distinct EffectIdRef or predeclared link substitutions and resolve from unique independent marker/identity bindings; no observed owner, recipient, body or status may be copied into expected predicates.
3. Preserve the pre-run logical manifest hash, approved plan/content hash and content-binding receipt, and independent ID-binding receipts; any concrete checker export receives its own hash. Resolving ApprovedContentRef freezes exact wording without changing Q01's independent semantic facts. Filling a permitted destination ID does not authorize arbitrary changes to approved bytes or a new semantic expectation.
4. Supply server-owned artifact/run success-claim scope templates for B07; models may propose content, but cannot assert verified execution. Runtime claim IDs/emission receipts are created later by the guarded workflow and reference actual verification records. The earlier no_affected path bypasses plan creation: its typed claim omits planRef and binds complete source/selection-empty and absence evidence, not a fabricated empty approved plan.
5. Extend tampering tests with ambiguous marker binding, content bound from observed Gmail bytes instead of the approved plan, wrong plan/content digest, undeclared substitution, changed citation/source digest, and a corrected draft whose first-proposal reference must remain unchanged.

**Additional acceptance:** B05 displays the exact approval-bound revision, B06 resolves only declared substitutions, and B07/Q02 can independently test the unchanged logical obligations. Q01 owns the evaluation oracle; a plan cannot reduce its expected artifact set or rewrite its expected outcome after execution.

## Agent, LLM and external-app integration

Use the [LLM call guide](../07-agent-spawning-and-llm-integration.md) and
[app transport guide](../08-mcp-api-and-external-app-integration.md) with the
[F02 handoff contract](../05-contracts-and-handoffs.md). This commit owns:

- `policy/claims.ts` and `plan.ts` consume A02 assessment, A03 draft and A04 findings through F02 types. This commit performs no LLM/network calls and must be testable with role fixtures before A02–A04 merge; R01 supplies the real results.
- Freeze exact approved bodies, recipients, app targets, effect payloads and permitted ID substitutions before B05 review. Cite the original role invocation/output references; do not ask an LLM to rewrite approved content during B06 REST/MCP dispatch.
- Reject a clean semantic audit used as approval, model-suggested tool/recipient expansion and body changes retaining an old plan hash. Preserve the original-output review record separately from approved content.

## Do not

- Regenerate text after approval, let model output override selected recipients,
  or use a hash as evidence that a factual claim is true.
- Include mutable plan revision or provider-created IDs in effect keys.
- Merge a new canonicalization convention without P1 and verifier review.

## Parallel work and sequential join

- **P3:** hashing/template/effect-key/plan source files.
- **P4:** `tests/app/plan.test.ts`, with independently expected hash behavior
  and exact-recipient/body tampering cases.
- **P1:** review persistence references and canonicalization compatibility,
  editing shared primitives only through an explicit foundation change.
- First agree canonical bytes/templates, then implement and join with test vectors.
  B05 approval and B06 execution consume only the merged exact-plan contract.

## Verification and handoff

Run typecheck/build and `npm run test:app -- tests/app/plan.test.ts`.
Prove changed content/source changes approval hash while logical effect identity
stays stable. Hand B05/B06 the stored full-hash/revision contract, approved-body
normalization, substitution rules, and original-proposal references.

## Completion receipt

- [x] B02 prerequisite: `a7a0fcb4d3b893595d1dc33c8afcf96926bc5cb6`.
- [x] Immutable plan implementation and adversarial/hash tests committed in the
   five allowed paths; no shared schema, dependency, adapter, or workflow file changed.
- [x] Conventions **1.2**, application schema **2**, plan policy **plan-v1**,
   canonical policy **plan-canonical-v1**, effect-key policy **effect-key-v1**.
- [x] Independent Explore boundary review found one CRLF/LF forbidden-claim
   normalization gap; follow-up verification accepted the B05/B06 handoff after correction.
- [x] Actual B03 implementation commit:
   `fe017b511b28cc8cef76c46d706019f3b6f56753`.

The implementation started from `6ca7dab44fefc300e9786d7043fe2d520e3b8d44`.
Review and compile corrections are
`f3a1eae4534174537670772af6dc224afcf080e8` and
`fe35ba5bf3fd0bf2f36778dfa54ec48dbd6b391c`; final code merge on `main` is
`bb7cbcafc001bcc72a2ebba11b01644440a920db`. Both `main` and
`feat/plan-policy` were published without force.

Final checks on the merged tree:

- `npm run test:app -- tests/app/plan.test.ts`: **8/8 passed**.
- `npm run typecheck`: passed.
- `npm run build:server`: passed.
- Staged-path and whitespace checks: passed.

Evidence is synthetic/offline. No full regression suite, live model/provider
call, authentic approval, protected mutation, human semantic review, or assembled
workflow was run. B05 consumes full plan hash/revision and claim templates; B06
consumes stable effect keys, exact request digests, and declared substitutions;
B07/Q02 consume the frozen content/provenance receipt without treating approval
or exact-byte equality as semantic truth.
