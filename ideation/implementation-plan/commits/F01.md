# F01 — Establish the tested application skeleton

**Branch:** `feat/foundation` · **Owner:** P1, backend/foundation integrator.
**Suggested commit subject:** `feat(F01): establish the tested application skeleton`
**Hard prerequisite:** [P00](P00.md), preserving the implemented monitor baseline.
This preservation gate is the deliberate foundation refinement recorded in the
[canonical backlog](../04-commit-plan.md).
**Branch sequence:** first commit in [feat/foundation](../branches/feat-foundation.md), followed by [F02](F02.md).

Use the [frontend pipeline and reliability guide](../09-frontend-pipeline-and-reliability.md)
for the browser build, test, static-serving, and secret-boundary requirements.

## Purpose and current state

**MERGED INTO LOCAL MAIN.** The existing package
now includes Fastify static serving and bootstrap health, a React/Vite entry,
server configuration validation, injected lifecycle hooks, and application,
component/accessibility and browser test harnesses. Local SQLite, LangGraph
checkpoint and Responses SDK compatibility tests are included. No product
workflow, provider adapter, model runtime, or operator console is implemented.
The completion receipt below records the checks and remaining handoff gates.

## Allowed paths

Root `package.json`, `package-lock.json`, `.node-version`, `tsconfig.json`,
`tsconfig.web.json`, `tsconfig.tests.json`,
`vite.config.ts`, `vitest.config.ts`, `playwright.config.ts`, `index.html`, `.env.example`,
`.gitignore`; `src/server/config.ts`, `src/server/index.ts`, `src/web/main.tsx`,
`tests/app/bootstrap.test.ts`, `tests/web/bootstrap.test.tsx`,
`tests/e2e/bootstrap.spec.ts`. Preserve the existing checker and monitor sources.

## Sequential implementation

1. Confirm P00's real baseline SHA and run the existing typecheck/build/checker
   and monitor checks. Inventory actual pinned versions before selecting additions.
2. Verify the proposed React/Vite, Fastify/Zod, LangGraph/LangChain/Gemini REST,
  SQLite/checkpointer, Vitest/Testing Library, automated accessibility, and
  Playwright APIs against the chosen compatible runtime. Pin only versions
  actually installed and smoke-tested; P1 alone changes the package and lockfile.
3. Add minimal backend/frontend bootstraps and environment validation without
  product-success routes. Keep provider/model credentials server-side and absent
  from examples. Add `build:server`, `build:web`, `test:app`, `test:web`, and
  `test:e2e` scripts and make aggregate `build`/`test` include the required new
  surfaces without silently skipping either one. Preserve the existing
  `test:checker` and `test:monitor` entry points.
4. Add bootstrap tests in this commit: health startup, useful configuration failure,
  browser entry render, production static-asset build/serve, one component test,
  one accessibility smoke, one 1440x900 Playwright smoke, and disposable
  application/checkpointer files opening through the chosen libraries. Schema
  ownership and product persistence belong to B01/B04; U01 owns product UI.
5. Run a clean dependency install, typecheck, server/web builds, bootstrap,
  component, accessibility and browser smokes, and existing checker/monitor
  regressions. Record exact script names, selected browser installation command,
  tested viewport, versions, and environment caveats for U01.

## Reliability implementation acceptance

This supplies the runnable foundation for the [reliability implementation plan](../06-agent-reliability-implementation.md); a health response does not close an agent reliability gate.

1. Preserve P00's checker/monitor entry points and clean-install receipt. Add explicit configuration boundaries for application storage, graph checkpoints, model/provider clients, and restricted evidence storage. Example configuration contains no credentials or fabricated review labels.
2. Keep model/provider clients injectable so Q04 can run the actual assembled graph against deterministic fake clients. Application startup must not silently substitute synthetic responses for unavailable live clients.
3. Register the application runner and shutdown hooks needed to flush committed events, stop scheduling, and reopen durable work. B01 owns transaction behavior and B04 owns scheduling; this commit proves only the bootstrap seams.
4. Document tested runtime/package versions and which commands exercise checker, monitor, application, and future scenarios. Missing optional export credentials must not disable local evidence recording.

**Additional acceptance:** a clean bootstrap with fake configuration has an explicit fixture identity; live configuration with missing required credentials fails before a model or provider dispatch. Existing checker and monitor suites remain passing. The production browser bundle contains no server secret and can be served from the same Fastify origin. Give F02 the tested dependency versions and configuration boundaries, U01 the component/accessibility/browser harness, and Q04 an injectable bootstrap without adding a new package or test runner in downstream branches.

## Agent, LLM and external-app integration

Use the [LLM call guide](../07-agent-spawning-and-llm-integration.md) and
[app transport guide](../08-mcp-api-and-external-app-integration.md) with the
[F02 handoff contract](../05-contracts-and-handoffs.md). This commit owns:

- In `src/server/index.ts`, validate server configuration from `.env.example`: `PG_MODEL_MODE=mock|live` and `PG_ADAPTER_MODE=fake|rest` are separate choices. Live model mode requires `GEMINI_API_KEY`, `GEMINI_MODEL` (optional per-role overrides); live apps require the four account/credential configurations in guide 08. Never expose these through Vite variables.
- Freeze proposed model budgets (`PG_MODEL_TIMEOUT_MS`, `PG_MODEL_ROLE_BUDGET_MS`, `PG_MODEL_MAX_ATTEMPTS`, `PG_MODEL_MAX_OUTPUT_TOKENS`, `PG_MODEL_MAX_INPUT_CHARS`) and the tested LangChain/graph plus Gemini REST contract versions. A01 implements calls; R01 constructs clients. Startup must not call models or mutate apps merely to check readiness.
- Keep MCP disabled for the REST MVP; any later MCP dependency/configuration lands through this owner after I01/per-app conformance. Bootstrap tests cover each explicit mode, missing keys, invalid budgets, and no secret/client leakage to browser or graph state.
- Own the registered `smoke:model` script/TypeScript runner configuration for A01's `tools/smoke/model.ts`. It is explicit opt-in live access and never part of default tests/bootstrap. A01 defines the smoke arguments and saves its bounded compatibility receipt; no extra implementation ID or merge prerequisite is added.

## Do not

- Add hosting, customer data, credentials, generic tool registries, or fake
  production completion.
- Rewrite the monitor or claim that a successful bootstrap is a four-app workflow.
- Let later feature branches independently edit root dependencies or lockfiles.

## Parallel work and sequential join

- **P1:** root configuration, dependency/lock changes, server bootstrap and web entry.
- **P4:** `tests/app/bootstrap.test.ts`, `tests/web/bootstrap.test.tsx`, and
  `tests/e2e/bootstrap.spec.ts`; independently check clean-start, browser render,
  accessibility, static serving, and viewport steps against the agreed bootstrap
  interfaces without changing root dependencies.
- **P2:** read-only compatibility/access review; hand findings to P1 without
  changing package files.
- Join after version selection: P1 integrates the bootstrap and tests, then runs
  the clean-install/build gate sequentially. F02 consumes the resulting toolchain.

## Verification and handoff

Run `npm run typecheck`, the server/web build scripts, `npm run test:checker`,
`npm run test:monitor`, and the newly implemented bootstrap, component and browser
smoke commands.
A sandbox subprocess failure is an unpassed check until rerun through the approved
path. Hand F02 the tested toolchain, script names, config conventions, and real SHA.

## Completion receipt

- [x] P00 is present on `main`: `37e1aa029439d7cd509b237525f8b8e02553528e`.
  Branch base: `dd37c38adc72c003f426b7b9e37c040f0a721485`.
- [x] Implementation and independently authored bootstrap tests are together in
  the local `feat/foundation` change.
- [x] Clean install, typecheck, both builds and **178/178 tests** pass: 129
  preserved checker/monitor, 46 application, 2 component/accessibility, 1 browser.
  No failed or skipped tests in the final aggregate run.
- [x] Independent helper review completed; fixed startup error masking during
  cleanup and added regressions for combined failures and failed stop hooks.
- [x] F02 receiving review accepted the tested F01 configuration/bootstrap seams.
  Conventions 1.2 preserves the existing index-based config and tests/web harness.
- [x] Actual F01 commit SHA, merged by fast-forward into local main: `9235026140db8d5083a9d2eabf5a91f73d37de5b`.

**Verification:** September 14, 2026, 02:04 IST. Node `24.21.0`, npm `11.19.0`.
The shell defaults to Node 18; this session placed
`/tmp/node-v24.21.0-linux-x64/bin` on `PATH`. Normal setup should activate the
version in `.node-version`. The selected test dependencies require Node 24.15+
within the supported Node 24 major.

Selected, installed and tested versions at the original F01 SHA (the later Gemini
migration supersedes only the model transport dependency; this historical receipt
is not rewritten):

- Fastify `5.12.4`, `@fastify/static` `10.1.3`, React/React DOM `19.3.0`,
  Vite `8.3.0`, `@vitejs/plugin-react` `6.1.1`.
- Zod `4.6.4`, TypeScript `7.0.2`, `@types/node` `24.13.4`.
- `@langchain/core` `1.2.11`, `@langchain/openai` `1.5.13`, LangGraph `1.4.15`,
  SQLite checkpointer `1.0.4`, resolved checkpoint core `1.1.5` and OpenAI SDK
  `7.15.0`. `better-sqlite3` `12.11.1` is shared by application/checkpointer;
  its exact install-script approval is recorded in `package.json` for npm 11.
- Vitest `5.0.0`, jsdom `30.0.1`, Testing Library React `16.3.3`, jest-dom
  `7.0.1`, axe-core / axe Playwright `4.13.0`, Playwright `1.63.0`.
  Chromium headless shell `153.0.8010.12`, revision `1243`, at **1440×900**.

Commands and results:

```bash
npm ci --offline --cache /tmp/promiseguard-npm-cache --no-audit --no-fund
npm run typecheck
npm test
npm run monitor -- demo --db /tmp/promiseguard-f01-monitor-demo.sqlite
npm ls --depth=0
git diff --check
```

All passed. The clean install uses this session's populated cache; ordinary
machines use `npm ci`. Aggregate `npm test` runs `build:server`, `build:web`,
`test:baseline`, `test:app`, `test:web`, and `test:e2e` without skipping surfaces.
`test:checker` and `test:monitor` remain available. The monitor demo remains one
synthetic supplied-evidence assessment. The final aggregate log is local at
`/tmp/promiseguard-f01-final-test.log`; the browser test writes its screenshot to
ignored `.local/playwright-results/`. Screenshot inspection found no clipping.

Browser installation was attempted with `npx playwright install chromium` and
`npx playwright install --only-shell chromium`. The sandbox attempt failed DNS;
approved downloader attempts timed out, including an IPv4 retry. The exact
official headless archive was then downloaded successfully with:

```bash
curl -fL --connect-timeout 15 --max-time 180 https://storage.googleapis.com/chrome-for-testing-public/153.0.8010.12/linux64/chrome-headless-shell-linux64.zip -o /tmp/promiseguard-chromium-headless-1243.zip
unzip -tq /tmp/promiseguard-chromium-headless-1243.zip
unzip -q /tmp/promiseguard-chromium-headless-1243.zip -d /home/biswaranjan/.cache/ms-playwright/chromium_headless_shell-1243
```

Archive integrity check and final browser smoke passed. Downloaded archive
SHA-256: `a9da028861a0cf789ff25c2fed45f5f1aaf969ed9247835b6a7821a4f7af9d1d`.
This manual cache path is an environment-specific fallback, not an application
dependency. Restricted CLI subprocess failures in the P00 check were rerun with
approved access; the final aggregate also used approved local subprocess/socket
access. Installation emitted the upstream `prebuild-install` deprecation notice;
the pinned native binding opened and reopened successfully. Early SDK test-double
and TypeScript failures were corrected before the final passing run.

**F02 / A01 / U01 / Q04 handoff:** `createApp({ env, webRoot, createServices })`
validates before service construction. Injected services support start, stop,
flush and close; R01 can close over its model/provider clients. F01 does not
construct business clients or run a scheduler. Shutdown preserves startup and
cleanup errors. The browser receives only static assets; `/api/health` projects
bootstrap scope, modes and synthetic fixture identity. Tests cover all four
mode combinations, every missing required live field, budgets, missing build,
secret isolation at build and runtime, absent product routes, and resource cleanup.
SQLite/checkpointer files are disposable compatibility probes, not B01 schemas.

The Responses smoke uses an injected local HTTP response and exercises prompt
formatting, strict schema request construction, SDK parsing and explicit Zod
validation. **A01 must independently apply the role schema to SDK output:** the
tested Responses path can return a parsed object that bypasses local Zod checks
inside `withStructuredOutput`. The negative fixture proves explicit validation
rejects it. No live model ID or schema/model combination is certified here.
Keep live model smoke opt-in when A01 adds its runner.

**Evidence mode:** local/synthetic only. Account access, Gmail OAuth, authentic
Slack replies, live model compatibility, product workflows, semantic review,
provider writes and live readback are **unrun**. F02 contracts and all downstream
implementation gates remain open. The historical monitor hash file stays intact;
19 of its 23 entries still match, with only F01's `.gitignore`, package files and
`tsconfig.json` intentionally changed. Current lockfile SHA-256:
`1aa782bf2c6fbe8bd55e92728c141c842f7990882d9e0ffd2c149e33670bff98`.
