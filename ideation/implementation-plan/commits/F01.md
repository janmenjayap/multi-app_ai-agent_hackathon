# F01 — Establish the tested application skeleton

**Branch:** `feat/foundation` · **Owner:** P1, backend/foundation integrator.
**Suggested commit subject:** `feat(F01): establish the tested application skeleton`
**Hard prerequisite:** [P00](P00.md), preserving the implemented monitor baseline.
This preservation gate is the deliberate foundation refinement recorded in the
[canonical backlog](../04-commit-plan.md).
**Branch sequence:** first commit in [feat/foundation](../branches/feat-foundation.md), followed by [F02](F02.md).

## Purpose and current state

**PARTIAL.** The Node/TypeScript package, locked dependencies, build/typecheck,
checker/monitor tests, and [standalone monitoring CLI](../../../tools/monitoring/README.md)
exist. Fastify, React/Vite, LangGraph/model wiring, and an application bootstrap
are still proposed. Extend this baseline; do not replace it with a second package.
This commit makes the application start and its chosen dependencies testable.

## Allowed paths

Root `package.json`, `package-lock.json`, `.node-version`, `tsconfig.json`,
`vite.config.ts`, `vitest.config.ts`, `index.html`, `.env.example`,
`.gitignore`; `src/server/index.ts`, `src/web/main.tsx`,
`tests/app/bootstrap.test.ts`. Preserve the existing checker and monitor sources.

## Sequential implementation

1. Confirm P00's real baseline SHA and run the existing typecheck/build/checker
   and monitor checks. Inventory actual pinned versions before selecting additions.
2. Verify the proposed React/Vite, Fastify/Zod, LangGraph/LangChain/OpenAI,
   SQLite/checkpointer, and application-test APIs against the chosen compatible
   runtime. Pin only versions actually installed and smoke-tested.
3. Add minimal backend/frontend bootstraps and environment validation without
   product-success routes. Keep provider/model credentials server-side and absent
   from examples. Add the application test script `npm run test:app -- <test-file>`.
4. Add bootstrap tests in this commit: health startup, useful configuration failure,
   frontend build, and disposable application/checkpointer files opening through
   the chosen libraries. Schema ownership and product persistence belong to B01/B04.
5. Run a clean dependency install, typecheck, both builds, bootstrap tests, and
   existing checker/monitor regressions. Record commands and environment caveats.

## Reliability implementation acceptance

This supplies the runnable foundation for the [reliability implementation plan](../06-agent-reliability-implementation.md); a health response does not close an agent reliability gate.

1. Preserve P00's checker/monitor entry points and clean-install receipt. Add explicit configuration boundaries for application storage, graph checkpoints, model/provider clients, and restricted evidence storage. Example configuration contains no credentials or fabricated review labels.
2. Keep model/provider clients injectable so Q04 can run the actual assembled graph against deterministic fake clients. Application startup must not silently substitute synthetic responses for unavailable live clients.
3. Register the application runner and shutdown hooks needed to flush committed events, stop scheduling, and reopen durable work. B01 owns transaction behavior and B04 owns scheduling; this commit proves only the bootstrap seams.
4. Document tested runtime/package versions and which commands exercise checker, monitor, application, and future scenarios. Missing optional export credentials must not disable local evidence recording.

**Additional acceptance:** a clean bootstrap with fake configuration has an explicit fixture identity; live configuration with missing required credentials fails before a model or provider dispatch. Existing checker and monitor suites remain passing. Give F02 the tested dependency versions and configuration boundaries, and give Q04 an injectable bootstrap without adding a new package or test runner in downstream branches.

## Agent, LLM and external-app integration

Use the [LLM call guide](../07-agent-spawning-and-llm-integration.md) and
[app transport guide](../08-mcp-api-and-external-app-integration.md) with the
[F02 handoff contract](../05-contracts-and-handoffs.md). This commit owns:

- In `src/server/index.ts`, validate server configuration from `.env.example`: `PG_MODEL_MODE=mock|live` and `PG_ADAPTER_MODE=fake|rest` are separate choices. Live model mode requires `OPENAI_API_KEY`, `OPENAI_MODEL` (optional per-role overrides); live apps require the four account/credential configurations in guide 08. Never expose these through Vite variables.
- Freeze proposed model budgets (`PG_MODEL_TIMEOUT_MS`, `PG_MODEL_ROLE_BUDGET_MS`, `PG_MODEL_MAX_ATTEMPTS`, `PG_MODEL_MAX_OUTPUT_TOKENS`, `PG_MODEL_MAX_INPUT_CHARS`) and the tested LangChain/OpenAI/graph package versions. A01 implements calls; R01 constructs clients. Startup must not call models or mutate apps merely to check readiness.
- Keep MCP disabled for the REST MVP; any later MCP dependency/configuration lands through this owner after I01/per-app conformance. Bootstrap tests cover each explicit mode, missing keys, invalid budgets, and no secret/client leakage to browser or graph state.
- Own the proposed `smoke:model` script/TypeScript runner configuration for A01's planned `tools/smoke/model.ts`. Register it when that entrypoint lands through a foundation-owned follow-up; it is explicit opt-in live access and never part of default tests/bootstrap. A01 defines the smoke arguments and saves its bounded compatibility receipt; no extra implementation ID or merge prerequisite is added.

## Do not

- Add hosting, customer data, credentials, generic tool registries, or fake
  production completion.
- Rewrite the monitor or claim that a successful bootstrap is a four-app workflow.
- Let later feature branches independently edit root dependencies or lockfiles.

## Parallel work and sequential join

- **P1:** root configuration and the two bootstrap source files.
- **P4:** `tests/app/bootstrap.test.ts`; independently check clean-start steps
  against the agreed bootstrap interfaces.
- **P2:** read-only compatibility/access review; hand findings to P1 without
  changing package files.
- Join after version selection: P1 integrates the bootstrap and tests, then runs
  the clean-install/build gate sequentially. F02 consumes the resulting toolchain.

## Verification and handoff

Run `npm run typecheck`, `npm run build`, `npm run test:checker`,
`npm run test:monitor`, and the newly implemented bootstrap test command.
A sandbox subprocess failure is an unpassed check until rerun through the approved
path. Hand F02 the tested toolchain, script names, config conventions, and real SHA.

## Completion receipt

- [ ] P00 merged; prerequisite SHA recorded: `UNRECORDED`.
- [ ] Implementation and bootstrap tests are in the same reviewed change.
- [ ] Exact commands, versions, pass/fail/unrun results and evidence: `UNRECORDED`.
- [ ] Reviewer and F02 handoff acceptance: `UNRECORDED`.
- [ ] Actual F01 commit SHA: `UNRECORDED`.
